# Plan 2 — 策略闭环补全 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 runtime 里写死的占位值(`daily_loss_pct=0.0`、`reverse_cross=False`、`holding_days=0`、仅 `enter_long`、参数不生效)换成真实闭环,使两套策略的熔断/反向交叉平仓/满6月调仓/做空/后台参数全部按需求文档真实生效。

**Architecture:** 不改运行时架构。状态闭环逻辑放进 `runtime_state.py`(纯逻辑、可单测),`runtime.py` 在既有调用点接入真值;新增 `params.py` 单一参数源,由 `AppState` 与 `LiveRuntime` 共享引用。所有新逻辑依赖注入/纯函数,远程可全测。

**Tech Stack:** Python 3.11, pytest, dataclasses。无新增第三方依赖。

## Global Constraints

- 测试从 `backend/` 目录运行:`.venv/bin/python -m pytest`。
- 不得在远程触发 `import vnpy*` 或网络;所有测试用 fake/构造数据。
- 现有 127 个测试必须保持全绿,不得回归。
- 纯逻辑放 `runtime_state.py` / `params.py`;`runtime.py` 只做接线。
- 熔断语义:单日账户亏损达 `max_daily_loss_pct`(默认 3.0)→ 当日闩锁,**剩余时间一律拦截开仓**,即使权益回升。
- 做空:仅对 `shortable=True`(universe 标记)的标的;不在名单的空头信号只记录不下单。
- 参数:`intraday_macd` / `trend_portfolio` 两组,默认值必须等于现有函数默认值(见各 Task)。
- commit message 用中文,前缀 `feat(live)`,结尾附 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

## File Structure

**Create:**
- `backend/quant/live/params.py` — `IntradayParams`、`PortfolioParams`、`LiveParams`(单一参数源)
- `backend/tests/quant/live/test_params.py`

**Modify:**
- `backend/quant/live/runtime_state.py` — 当日权益基线+熔断闩锁;holding_days 建仓日期跟踪+启动回放纯函数
- `backend/quant/live/runtime.py` — 接入 daily_loss/熔断、reverse_cross、holding_days、做空、参数
- `backend/quant/live/risk.py` — `evaluate_live_order_risk` 增加 `account_halted` 拦截(熔断闩锁)
- `backend/app/services/state.py` — `AppState` 持有并更新共享 `LiveParams`
- `backend/app/main.py` — 构造共享 `LiveParams` 注入 runtime 与 AppState
- 对应测试:`test_runtime_state.py`、`test_runtime.py`、`test_risk.py`、`test_live_state_overlay.py`(如涉及)

---

### Task 1: runtime_state 当日权益基线 + 熔断闩锁

**Files:**
- Modify: `backend/quant/live/runtime_state.py`
- Test: `backend/tests/quant/live/test_runtime_state.py`

**Interfaces:**
- Produces on `StrategyRuntimeState`:
  - `observe_account_equity(balance: float, day: date) -> None` — 当日首个正余额记为基线
  - `daily_loss_pct(current_balance: float) -> float` — `(current-baseline)/baseline*100`;无基线或基线≤0 返回 `0.0`
  - `trip_halt_if_breached(current_balance: float, max_daily_loss_pct: float) -> bool` — 达阈值则置 `halted_today=True` 并返回 True
  - `is_halted() -> bool`
  - `reset_for_day(day)` 额外清空 `day_start_equity` 与 `halted_today`

- [ ] **Step 1: 写失败测试** — 追加到 `backend/tests/quant/live/test_runtime_state.py`:

```python
def test_equity_baseline_and_daily_loss_pct():
    from datetime import date
    from quant.live.runtime_state import StrategyRuntimeState

    state = StrategyRuntimeState()
    day = date(2026, 6, 24)
    state.observe_account_equity(1000.0, day)
    state.observe_account_equity(900.0, day)  # 基线只取当日第一次
    assert state.daily_loss_pct(900.0) == -10.0


def test_daily_loss_pct_without_baseline_is_zero():
    from quant.live.runtime_state import StrategyRuntimeState

    assert StrategyRuntimeState().daily_loss_pct(900.0) == 0.0


def test_trip_halt_latches_until_day_reset():
    from datetime import date
    from quant.live.runtime_state import StrategyRuntimeState

    state = StrategyRuntimeState()
    day = date(2026, 6, 24)
    state.observe_account_equity(1000.0, day)
    assert state.trip_halt_if_breached(965.0, 3.0) is True  # -3.5% 触发
    assert state.is_halted() is True
    assert state.trip_halt_if_breached(1000.0, 3.0) is True  # 回升仍闩锁
    assert state.is_halted() is True
    state.reset_for_day(date(2026, 6, 25))
    assert state.is_halted() is False


def test_trip_halt_not_breached():
    from datetime import date
    from quant.live.runtime_state import StrategyRuntimeState

    state = StrategyRuntimeState()
    state.observe_account_equity(1000.0, date(2026, 6, 24))
    assert state.trip_halt_if_breached(980.0, 3.0) is False  # -2%
    assert state.is_halted() is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime_state.py -q`
Expected: FAIL（`AttributeError: 'StrategyRuntimeState' object has no attribute 'observe_account_equity'`）

- [ ] **Step 3: 实现** — 在 `StrategyRuntimeState` 加字段与方法。

字段区(在 `_current_day` 附近)新增:

```python
    day_start_equity: float | None = None
    halted_today: bool = False
```

`reset_for_day` 改为同时清空基线与闩锁:

```python
    def reset_for_day(self, day: date) -> None:
        if self._current_day == day:
            return
        self._current_day = day
        self.stopped_symbols_today.clear()
        self.intraday_half_taken.clear()
        self.day_start_equity = None
        self.halted_today = False
```

新增方法:

```python
    def observe_account_equity(self, balance: float, day: date) -> None:
        self.reset_for_day(day)
        if self.day_start_equity is None and balance > 0:
            self.day_start_equity = float(balance)

    def daily_loss_pct(self, current_balance: float) -> float:
        if not self.day_start_equity or self.day_start_equity <= 0:
            return 0.0
        return (float(current_balance) - self.day_start_equity) / self.day_start_equity * 100

    def trip_halt_if_breached(self, current_balance: float, max_daily_loss_pct: float) -> bool:
        if self.daily_loss_pct(current_balance) <= -abs(max_daily_loss_pct):
            self.halted_today = True
        return self.halted_today

    def is_halted(self) -> bool:
        return self.halted_today
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime_state.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/runtime_state.py backend/tests/quant/live/test_runtime_state.py
git commit -m "feat(live): 当日权益基线与熔断闩锁状态

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: risk 熔断拦截 + runtime 接入真实 daily_loss/熔断

**Files:**
- Modify: `backend/quant/live/risk.py`, `backend/quant/live/runtime.py`
- Test: `backend/tests/quant/live/test_risk.py`, `backend/tests/quant/live/test_runtime.py`

**Interfaces:**
- Consumes: Task 1 的 `observe_account_equity`/`daily_loss_pct`/`trip_halt_if_breached`/`is_halted`。
- Produces: `evaluate_live_order_risk(..., account_halted: bool = False)` — `account_halted=True` 且 `purpose=="open"` 时拦截。runtime `run_once` 在每轮观察账户权益并在达阈值时闩锁;`_run_intraday_entries`/`_run_portfolio_entries`/`_live_risk` 传真实 `daily_loss_pct` 与 `account_halted`。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_risk.py`:

```python
def test_live_risk_blocks_open_when_account_halted():
    from quant.live.risk import evaluate_live_order_risk

    decision = evaluate_live_order_risk(
        symbol="AAPL",
        market="US",
        purpose="open",
        gateway_connected=True,
        daily_loss_pct=0.0,
        stopped_symbols_today=(),
        account_halted=True,
    )
    assert decision.allowed is False
    assert any("熔断" in reason for reason in decision.reasons)


def test_live_risk_halt_does_not_block_close():
    from quant.live.risk import evaluate_live_order_risk

    decision = evaluate_live_order_risk(
        symbol="AAPL",
        market="US",
        purpose="close",
        gateway_connected=True,
        daily_loss_pct=0.0,
        stopped_symbols_today=(),
        account_halted=True,
    )
    assert decision.allowed is True
```

追加到 `backend/tests/quant/live/test_runtime.py`（沿用文件顶部 `_runtime`/`FakeDataProvider`/`FakeMarketData` 夹具）:

```python
def test_run_once_trips_halt_on_daily_loss(tmp_path):
    from datetime import date
    from quant.live.translate import GatewayAccount

    runtime, _gateway = _runtime(tmp_path)
    day = date(2026, 6, 24)
    runtime.runtime_state.observe_account_equity(1000.0, day)
    runtime.live_state.update_account(GatewayAccount("acc", 950.0, 950.0, 0.0))

    runtime.run_once(datetime(2026, 6, 24, 14, 0, tzinfo=timezone.utc))

    assert runtime.runtime_state.is_halted() is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_risk.py tests/quant/live/test_runtime.py -q`
Expected: FAIL（`TypeError: evaluate_live_order_risk() got an unexpected keyword argument 'account_halted'`，runtime 测试 `assert is_halted()` 为 False）

- [ ] **Step 3: 实现**

`backend/quant/live/risk.py` — `evaluate_live_order_risk` 签名加 `account_halted: bool = False`,在 `purpose == "open"` 分支首部拦截:

```python
def evaluate_live_order_risk(
    symbol: str,
    market: Market,
    purpose: OrderPurpose,
    gateway_connected: bool,
    daily_loss_pct: float,
    stopped_symbols_today: Sequence[str],
    max_daily_loss_pct: float = 3.0,
    pdt_trades_remaining: int | None = None,
    is_short: bool = False,
    shortable: bool = True,
    consecutive_order_failures: int = 0,
    max_order_failures: int = 3,
    account_halted: bool = False,
) -> LiveRiskDecision:
    blocks: list[str] = []
    normalized_symbol = symbol.strip().upper()
    stopped_symbols = {item.strip().upper() for item in stopped_symbols_today}

    if not gateway_connected:
        blocks.append("网关未连接，暂停下单")
    if consecutive_order_failures >= max_order_failures:
        blocks.append("连续下单失败次数过多，暂停自动下单")

    if purpose == "open":
        if account_halted:
            blocks.append("已触发当日熔断，停止开仓")
        if daily_loss_pct <= -abs(max_daily_loss_pct):
            blocks.append("触发单日账户最大亏损")
        if normalized_symbol in stopped_symbols:
            blocks.append("该标的当日已止损，禁止再开仓")
        if market == "US" and pdt_trades_remaining is not None and pdt_trades_remaining <= 0:
            blocks.append("美股 PDT 日内交易额度不足")
        if is_short and not shortable:
            blocks.append("标的不满足可做空校验")

    return LiveRiskDecision(
        allowed=not blocks,
        reasons=tuple(blocks or ["实盘风控通过"]),
    )
```

`backend/quant/live/runtime.py`:

1) `run_once`(在 `persist_gateway_snapshot` 之前)加权益观察+闩锁:

```python
    def run_once(self, at: datetime) -> None:
        snapshot = self.live_state.snapshot()
        self.runtime_state.reset_for_day(at.date())
        if self._seeded_day != at.date():
            self._seeded_day = at.date()
            self._seeded_symbols.clear()
        self.market_data.ingest_ticks(snapshot.get("ticks", []))
        self._observe_account(snapshot, at)
        self.runtime_state.persist_gateway_snapshot(snapshot, at, self.db_path)
        for action in self.scheduler.due_actions(at):
            self.handle_action(action, at)

    def _observe_account(self, snapshot: dict[str, Any], at: datetime) -> None:
        account = snapshot.get("account")
        if account is None:
            return
        self.runtime_state.observe_account_equity(float(account.balance), at.date())
        self.runtime_state.trip_halt_if_breached(float(account.balance), self.config.max_daily_loss_pct)
```

2) `RuntimeConfig` 加字段 `max_daily_loss_pct: float = 3.0`（在 `default_equity` 后）。

3) `_run_intraday_entries` 把 `daily_loss_pct=0.0`(行 191)替换为真实值:

```python
                daily_loss_pct=self.runtime_state.daily_loss_pct(_account_equity(snapshot, self.config.default_equity)),
```

4) `_live_risk` 替换 `daily_loss_pct=0.0`(行 344）并新增 `account_halted`:

```python
    def _live_risk(self, symbol: str, market: Market, purpose: str, at: datetime):
        snapshot = self.live_state.snapshot()
        balance = _account_equity(snapshot, self.config.default_equity)
        return evaluate_live_order_risk(
            symbol=symbol,
            market=market,
            purpose=purpose,
            gateway_connected=bool(snapshot.get("connected")),
            daily_loss_pct=self.runtime_state.daily_loss_pct(balance),
            stopped_symbols_today=tuple(self.runtime_state.stopped_symbols_today),
            pdt_trades_remaining=self.runtime_state.pdt_remaining(at.date()) if market == "US" else None,
            consecutive_order_failures=self.runtime_state.consecutive_order_failures,
            account_halted=self.runtime_state.is_halted(),
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_risk.py tests/quant/live/test_runtime.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/risk.py backend/quant/live/runtime.py backend/tests/quant/live/test_risk.py backend/tests/quant/live/test_runtime.py
git commit -m "feat(live): 接入真实当日亏损与熔断闩锁拦截开仓

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: MACD 反向交叉平仓

**Files:**
- Modify: `backend/quant/live/runtime.py`
- Test: `backend/tests/quant/live/test_runtime.py`

**Interfaces:**
- Consumes: `quant.indicators.macd.macd`/`has_bullish_cross`/`has_bearish_cross`（已存在）;`BarAggregator.interval_bars`。
- Produces: `LiveRuntime._reverse_cross(symbol: str, side: str) -> bool` — 多头看死叉、空头看金叉;`_run_intraday_exits` 用其真值替换写死的 `reverse_cross=False`。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_runtime.py`:

```python
def test_reverse_cross_long_detects_bearish_cross(tmp_path):
    runtime, _gateway = _runtime(tmp_path)
    # 构造一段先涨后急跌的 15m 收盘价，使 MACD 形成死叉
    closes = [100 + i for i in range(30)] + [130 - i * 3 for i in range(10)]
    bars = [
        Bar("AAPL", datetime(2026, 6, 24, 9, i, tzinfo=timezone.utc), c, c + 1, c - 1, c, 100)
        for i, c in enumerate(closes)
    ]

    class _MD:
        def interval_bars(self, symbol, interval_minutes, limit=100):
            return bars

    runtime.market_data = _MD()
    assert runtime._reverse_cross("AAPL", "long") is True


def test_reverse_cross_insufficient_bars_is_false(tmp_path):
    runtime, _gateway = _runtime(tmp_path)

    class _MD:
        def interval_bars(self, symbol, interval_minutes, limit=100):
            return []

    runtime.market_data = _MD()
    assert runtime._reverse_cross("AAPL", "long") is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: FAIL（`AttributeError: 'LiveRuntime' object has no attribute '_reverse_cross'`）

- [ ] **Step 3: 实现**

`backend/quant/live/runtime.py`:顶部 import 追加:

```python
from quant.indicators.macd import has_bearish_cross, has_bullish_cross, macd
```

新增方法:

```python
    def _reverse_cross(self, symbol: str, side: str) -> bool:
        bars15 = self.market_data.interval_bars(symbol, 15, limit=80)
        points = macd([bar.close for bar in bars15])
        if len(points) < 2:
            return False
        return has_bearish_cross(points) if side == "long" else has_bullish_cross(points)
```

`_run_intraday_exits`:把首次 `evaluate_intraday_exit_signal(... reverse_cross=False)`(行 218)改为:

```python
                reverse_cross=self._reverse_cross(position.symbol, _position_side(position.direction)),
```

(`force` 分支的二次评估不需要 reverse_cross，保持不变。)

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/runtime.py backend/tests/quant/live/test_runtime.py
git commit -m "feat(live): 日内出场接入真实 MACD 反向交叉

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: runtime_state holding_days 建仓日期跟踪 + 启动回放纯函数

**Files:**
- Modify: `backend/quant/live/runtime_state.py`
- Test: `backend/tests/quant/live/test_runtime_state.py`

**Interfaces:**
- Produces on `StrategyRuntimeState`:
  - 字段 `portfolio_entry_dates: dict[str, date]`
  - `record_portfolio_entry_date(symbol: str, day: date) -> None` — 仅首次记录（已存在不覆盖）
  - `holding_days(symbol: str, today: date) -> int` — 无记录返回 `0`
  - `load_entry_dates_from_events(events) -> None` — 从 store 的 trade 事件回放：每标的最早一笔「开」成交日期
- 模块级纯函数 `entry_dates_from_trade_events(events) -> dict[str, date]`（供回放复用、可单测）。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_runtime_state.py`:

```python
def test_record_and_compute_holding_days():
    from datetime import date
    from quant.live.runtime_state import StrategyRuntimeState

    state = StrategyRuntimeState()
    state.record_portfolio_entry_date("0700.HK", date(2026, 1, 1))
    state.record_portfolio_entry_date("0700.HK", date(2026, 2, 1))  # 不覆盖
    assert state.holding_days("0700.HK", date(2026, 1, 31)) == 30
    assert state.holding_days("UNKNOWN", date(2026, 1, 31)) == 0


def test_entry_dates_from_trade_events_earliest_open():
    from datetime import datetime, timezone
    from types import SimpleNamespace
    from quant.live.runtime_state import entry_dates_from_trade_events

    def ev(symbol, offset, day):
        return SimpleNamespace(
            symbol=symbol,
            created_at=datetime(2026, day // 100, day % 100, tzinfo=timezone.utc),
            payload={"offset": offset},
        )

    events = [
        ev("0700.HK", "开", 215),  # 2026-02-15
        ev("0700.HK", "开", 110),  # 2026-01-10  更早
        ev("AAPL", "平", 120),     # 平仓不计
    ]
    result = entry_dates_from_trade_events(events)
    assert result["0700.HK"].day == 10 and result["0700.HK"].month == 1
    assert "AAPL" not in result
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime_state.py -q`
Expected: FAIL（`AttributeError`/`ImportError: cannot import name 'entry_dates_from_trade_events'`）

- [ ] **Step 3: 实现**

`backend/quant/live/runtime_state.py`:字段区新增:

```python
    portfolio_entry_dates: dict[str, date] = field(default_factory=dict)
```

新增方法:

```python
    def record_portfolio_entry_date(self, symbol: str, day: date) -> None:
        self.portfolio_entry_dates.setdefault(_normalize(symbol), day)

    def holding_days(self, symbol: str, today: date) -> int:
        entry = self.portfolio_entry_dates.get(_normalize(symbol))
        return (today - entry).days if entry is not None else 0

    def load_entry_dates_from_events(self, events) -> None:
        for symbol, day in entry_dates_from_trade_events(events).items():
            self.portfolio_entry_dates.setdefault(symbol, day)
```

文件末尾(`_normalize` 附近)新增模块级纯函数:

```python
def entry_dates_from_trade_events(events) -> dict[str, date]:
    result: dict[str, date] = {}
    for event in sorted(events, key=lambda item: item.created_at):
        payload = getattr(event, "payload", {}) or {}
        offset = str(payload.get("offset", ""))
        symbol = getattr(event, "symbol", None) or payload.get("symbol")
        if symbol and "开" in offset:
            result.setdefault(_normalize(symbol), event.created_at.date())
    return result
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime_state.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/runtime_state.py backend/tests/quant/live/test_runtime_state.py
git commit -m "feat(live): 中长线建仓日期跟踪与成交回放重建

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: runtime 接入 holding_days + 启动回放

**Files:**
- Modify: `backend/quant/live/runtime.py`
- Test: `backend/tests/quant/live/test_runtime.py`

**Interfaces:**
- Consumes: Task 4 的 `record_portfolio_entry_date`/`holding_days`/`load_entry_dates_from_events`;`quant.live.store.list_live_events`。
- Produces: `LiveRuntime` 在 `start()` 回放建仓日期;`_run_portfolio_entries` 首次建仓成交后记录日期;`_run_portfolio_exits` 传真实 `holding_days`。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_runtime.py`:

```python
def test_portfolio_entry_records_entry_date(tmp_path, monkeypatch):
    from types import SimpleNamespace

    runtime, gateway = _runtime(tmp_path)
    runtime.runtime_state.portfolio_watchlist = ["0700.HK"]

    monkeypatch.setattr(
        "quant.live.runtime.evaluate_trend_entry_signal",
        lambda symbol, timing, stage: SimpleNamespace(
            action="enter_first", symbol=symbol, stage="first", pullback_confirmed=True
        ),
    )

    class _DP(FakeDataProvider):
        def daily_timing(self, symbol, market):
            return SimpleNamespace(close=100.0)

    runtime.data_provider = _DP()
    at = datetime(2026, 6, 24, 20, 5, tzinfo=timezone.utc)
    runtime._run_portfolio_entries("HK", at)

    assert runtime.runtime_state.holding_days("0700.HK", at.date()) == 0
    assert "0700.HK" in runtime.runtime_state.portfolio_entry_dates
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: FAIL（`portfolio_entry_dates` 为空 → `assert "0700.HK" in ...` 失败）

- [ ] **Step 3: 实现**

`backend/quant/live/runtime.py`:

1) `_run_portfolio_entries`:在 `mark_portfolio_entry_submitted` 之后记录建仓日期:

```python
            if result.submitted:
                self.runtime_state.mark_portfolio_entry_submitted(symbol, signal.stage)
                if signal.stage == "first":
                    self.runtime_state.record_portfolio_entry_date(symbol, at.date())
```

2) `_run_portfolio_exits`:把 `holding_days=0`(行 312)替换为:

```python
                    holding_days=self.runtime_state.holding_days(position.symbol, at.date()),
```

3) `start()`:在 `self._running = True` 之前回放建仓日期(import 在文件顶部已有 `from quant.live.store import ... record_live_event`,追加 `list_live_events`):

顶部 import 改为:

```python
from quant.live.store import DbPath, list_live_events, record_live_event
```

`start()` 内 `self.gateway.subscribe(...)` 之后、`self._running = True` 之前:

```python
        self.runtime_state.load_entry_dates_from_events(
            list_live_events(kind="trade", db_path=self.db_path)
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/runtime.py backend/tests/quant/live/test_runtime.py
git commit -m "feat(live): 中长线出场接入真实持仓天数与启动回放

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: 日内做空接线

**Files:**
- Modify: `backend/quant/live/runtime.py`
- Test: `backend/tests/quant/live/test_runtime.py`

**Interfaces:**
- Consumes: `quant.data.universe.all_symbols`（`SymbolInfo.shortable`,Plan 1 Task 1);`evaluate_intraday_entry_signal(side=...)`、`execute_intraday_entry(is_short=..., shortable=...)`（已存在参数)。
- Produces: `LiveRuntime._is_shortable(symbol) -> bool`;`_run_intraday_entries` 在多头不成立时,对可做空标的评估空头并下单;不可做空的空头信号只落 signal 日志。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_runtime.py`:

```python
def test_intraday_short_entry_for_shortable_symbol(tmp_path, monkeypatch):
    from types import SimpleNamespace

    runtime, gateway = _runtime(tmp_path)
    runtime.runtime_state.intraday_watchlist = ["AAPL"]  # universe 中 AAPL shortable=True

    def fake_signal(**kwargs):
        if kwargs.get("side") == "short":
            return SimpleNamespace(action="enter_short")
        return SimpleNamespace(action="wait")

    monkeypatch.setattr("quant.live.runtime.evaluate_intraday_entry_signal", fake_signal)

    runtime._run_intraday_entries("US", datetime(2026, 6, 24, 14, 15, tzinfo=timezone.utc))
    snapshot = runtime.live_state.snapshot()

    assert snapshot["positions"][0].symbol == "AAPL"
    assert "空" in snapshot["positions"][0].direction


def test_intraday_short_blocked_for_non_shortable(tmp_path, monkeypatch):
    from types import SimpleNamespace

    runtime, gateway = _runtime(tmp_path)
    runtime.runtime_state.intraday_watchlist = ["NVDA"]  # universe 中 NVDA shortable=False

    monkeypatch.setattr(
        "quant.live.runtime.evaluate_intraday_entry_signal",
        lambda **kwargs: SimpleNamespace(action="enter_short") if kwargs.get("side") == "short" else SimpleNamespace(action="wait"),
    )

    runtime._run_intraday_entries("US", datetime(2026, 6, 24, 14, 15, tzinfo=timezone.utc))
    assert runtime.live_state.snapshot()["positions"] == []
```

注意：`FakeMarketData.interval_bars` 返回 26 根 bar，满足 `len(bars) >= 26` 入场前置;`evaluate_intraday_entry_signal` 被 mock，不依赖真实 MACD。

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: FAIL（当前仅处理 `enter_long`，无空头持仓 → 两个断言失败）

- [ ] **Step 3: 实现**

`backend/quant/live/runtime.py`:顶部 import 追加:

```python
from quant.data.universe import all_symbols
```

`LiveRuntime.__init__` 末尾新增可做空集合:

```python
        self._shortable_symbols = {item.symbol.upper() for item in all_symbols() if item.shortable}
```

新增方法:

```python
    def _is_shortable(self, symbol: str) -> bool:
        return symbol.upper() in self._shortable_symbols
```

`_run_intraday_entries` 把判定段(从 `signal = evaluate_intraday_entry_signal(...)` 到 `execute_intraday_entry(...)`)改为:先评估多头;多头不成立且可做空时评估空头;然后据 `is_short` 下单。即将原:

```python
            signal = evaluate_intraday_entry_signal(
                symbol=symbol, market=market, at=at,
                closes_15m=closes15, lows_15m=[bar.low for bar in bars15],
                highs_15m=[bar.high for bar in bars15], closes_5m=[bar.close for bar in bars5],
                current_price=price, ma5_15m=sum(closes15[-5:]) / 5,
            )
            if signal.action != "enter_long":
                continue
            risk = self._live_risk(symbol, market, "open", at)
            if not risk.allowed:
                self._record_signal("intraday_macd", symbol, risk.reasons, at)
                continue
            result = execute_intraday_entry(
                gateway=self.gateway, symbol=symbol, price=price,
                total_equity=_account_equity(snapshot, self.config.default_equity),
                current_symbols=_current_symbols(snapshot),
                stopped_symbols_today=tuple(self.runtime_state.stopped_symbols_today),
                daily_loss_pct=self.runtime_state.daily_loss_pct(_account_equity(snapshot, self.config.default_equity)),
                pdt_trades_remaining=self.runtime_state.pdt_remaining(at.date()) if market == "US" else None,
            )
```

替换为:

```python
            def _entry(side: str):
                return evaluate_intraday_entry_signal(
                    symbol=symbol, market=market, at=at,
                    closes_15m=closes15, lows_15m=[bar.low for bar in bars15],
                    highs_15m=[bar.high for bar in bars15], closes_5m=[bar.close for bar in bars5],
                    current_price=price, ma5_15m=sum(closes15[-5:]) / 5, side=side,
                    shortable=self._is_shortable(symbol),
                )

            signal = _entry("long")
            is_short = False
            if signal.action != "enter_long" and self._is_shortable(symbol):
                short_signal = _entry("short")
                if short_signal.action == "enter_short":
                    signal, is_short = short_signal, True
            if signal.action not in ("enter_long", "enter_short"):
                continue
            risk = self._live_risk(symbol, market, "open", at, is_short=is_short)
            if not risk.allowed:
                self._record_signal("intraday_macd", symbol, risk.reasons, at)
                continue
            equity = _account_equity(snapshot, self.config.default_equity)
            result = execute_intraday_entry(
                gateway=self.gateway, symbol=symbol, price=price,
                total_equity=equity,
                current_symbols=_current_symbols(snapshot),
                stopped_symbols_today=tuple(self.runtime_state.stopped_symbols_today),
                daily_loss_pct=self.runtime_state.daily_loss_pct(equity),
                pdt_trades_remaining=self.runtime_state.pdt_remaining(at.date()) if market == "US" else None,
                is_short=is_short,
                shortable=self._is_shortable(symbol),
            )
```

并把 `_live_risk` 签名扩展为接受 `is_short`（默认 False）并透传:

```python
    def _live_risk(self, symbol: str, market: Market, purpose: str, at: datetime, is_short: bool = False):
        snapshot = self.live_state.snapshot()
        balance = _account_equity(snapshot, self.config.default_equity)
        return evaluate_live_order_risk(
            symbol=symbol,
            market=market,
            purpose=purpose,
            gateway_connected=bool(snapshot.get("connected")),
            daily_loss_pct=self.runtime_state.daily_loss_pct(balance),
            stopped_symbols_today=tuple(self.runtime_state.stopped_symbols_today),
            pdt_trades_remaining=self.runtime_state.pdt_remaining(at.date()) if market == "US" else None,
            consecutive_order_failures=self.runtime_state.consecutive_order_failures,
            account_halted=self.runtime_state.is_halted(),
            is_short=is_short,
            shortable=self._is_shortable(symbol),
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: PASS

- [ ] **Step 5: 全量回归 + 提交**

```bash
.venv/bin/python -m pytest -q
git add backend/quant/live/runtime.py backend/tests/quant/live/test_runtime.py
git commit -m "feat(live): 日内做空接线(可做空名单门控)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: LiveParams 参数源

**Files:**
- Create: `backend/quant/live/params.py`
- Test: `backend/tests/quant/live/test_params.py`

**Interfaces:**
- Produces:
  - `IntradayParams`（frozen dataclass，默认值=现有函数默认）:`stop_loss_pct=1.5`、`take_profit_1_pct=2.0`、`take_profit_2_pct=3.5`、`position_fraction_pct=10.0`、`max_positions=3`、`max_daily_loss_pct=3.0`
  - `PortfolioParams`（frozen dataclass）:`single_position_cap_pct=15.0`、`target_positions_max=8`、`first_entry_fraction_pct=60.0`、`max_symbol_drawdown_pct=18.0`、`take_profit_pct=20.0`、`rebalance_months=6`、`hot_gain_block_pct=40.0`
  - `LiveParams`（可变持有者）:`.intraday`、`.portfolio`;`update(strategy_id: str, values: Mapping[str, Any]) -> None`（仅接受已知字段、忽略未知键、数值型）

- [ ] **Step 1: 写失败测试**

`backend/tests/quant/live/test_params.py`:

```python
from __future__ import annotations

from quant.live.params import IntradayParams, LiveParams, PortfolioParams


def test_defaults_match_strategy_function_defaults():
    intraday = IntradayParams()
    assert intraday.stop_loss_pct == 1.5
    assert intraday.take_profit_2_pct == 3.5
    assert intraday.position_fraction_pct == 10.0
    assert intraday.max_positions == 3
    portfolio = PortfolioParams()
    assert portfolio.single_position_cap_pct == 15.0
    assert portfolio.rebalance_months == 6


def test_update_intraday_params():
    params = LiveParams()
    params.update("intraday_macd", {"stop_loss_pct": 2.0, "max_positions": 5})
    assert params.intraday.stop_loss_pct == 2.0
    assert params.intraday.max_positions == 5
    assert params.intraday.take_profit_1_pct == 2.0  # 未改动保持默认


def test_update_ignores_unknown_keys():
    params = LiveParams()
    params.update("intraday_macd", {"bogus": 9, "stop_loss_pct": 1.0})
    assert params.intraday.stop_loss_pct == 1.0
    assert not hasattr(params.intraday, "bogus")


def test_update_portfolio_params():
    params = LiveParams()
    params.update("trend_portfolio", {"take_profit_pct": 25.0})
    assert params.portfolio.take_profit_pct == 25.0


def test_update_unknown_strategy_is_noop():
    params = LiveParams()
    params.update("nope", {"stop_loss_pct": 99})
    assert params.intraday.stop_loss_pct == 1.5
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_params.py -q`
Expected: FAIL（`ModuleNotFoundError: quant.live.params`）

- [ ] **Step 3: 实现**

`backend/quant/live/params.py`:

```python
from __future__ import annotations

from dataclasses import dataclass, fields, replace
from typing import Any, Mapping


@dataclass(frozen=True)
class IntradayParams:
    stop_loss_pct: float = 1.5
    take_profit_1_pct: float = 2.0
    take_profit_2_pct: float = 3.5
    position_fraction_pct: float = 10.0
    max_positions: int = 3
    max_daily_loss_pct: float = 3.0


@dataclass(frozen=True)
class PortfolioParams:
    single_position_cap_pct: float = 15.0
    target_positions_max: int = 8
    first_entry_fraction_pct: float = 60.0
    max_symbol_drawdown_pct: float = 18.0
    take_profit_pct: float = 20.0
    rebalance_months: int = 6
    hot_gain_block_pct: float = 40.0


_INTRADAY_FIELDS = {f.name: f.type for f in fields(IntradayParams)}
_PORTFOLIO_FIELDS = {f.name: f.type for f in fields(PortfolioParams)}


class LiveParams:
    def __init__(self) -> None:
        self.intraday = IntradayParams()
        self.portfolio = PortfolioParams()

    def update(self, strategy_id: str, values: Mapping[str, Any]) -> None:
        if strategy_id == "intraday_macd":
            self.intraday = _merge(self.intraday, values, _INTRADAY_FIELDS)
        elif strategy_id == "trend_portfolio":
            self.portfolio = _merge(self.portfolio, values, _PORTFOLIO_FIELDS)


def _merge(current, values: Mapping[str, Any], known: dict[str, Any]):
    changes: dict[str, Any] = {}
    for key, value in values.items():
        if key not in known:
            continue
        changes[key] = int(value) if known[key] in ("int", int) else float(value)
    return replace(current, **changes) if changes else current
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_params.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/params.py backend/tests/quant/live/test_params.py
git commit -m "feat(live): 实盘参数单一源(intraday/portfolio)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: runtime 与 AppState 接入共享 LiveParams

**Files:**
- Modify: `backend/quant/live/runtime.py`, `backend/app/services/state.py`, `backend/app/main.py`
- Test: `backend/tests/quant/live/test_runtime.py`, `backend/tests/test_live_state_overlay.py`

**Interfaces:**
- Consumes: Task 7 的 `LiveParams`。
- Produces: `LiveRuntime(..., params: LiveParams | None = None)`,策略/执行器调用点读 `self.params`;`build_live_runtime_from_env(live_state, params=None)`;`AppState(live_state, params=None)`,`update_strategy_params` 同步更新 `self.params`;`main.py` 构造一个 `LiveParams` 注入两者。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_runtime.py`:

```python
def test_runtime_uses_injected_params_for_entry(tmp_path, monkeypatch):
    from types import SimpleNamespace
    from quant.live.params import LiveParams

    params = LiveParams()
    params.update("intraday_macd", {"position_fraction_pct": 25.0})

    runtime, _gateway = _runtime(tmp_path)
    runtime.params = params
    runtime.runtime_state.intraday_watchlist = ["AAPL"]

    captured = {}

    def fake_execute(**kwargs):
        captured.update(kwargs)
        return SimpleNamespace(submitted=False, reasons=("x",))

    monkeypatch.setattr("quant.live.runtime.evaluate_intraday_entry_signal", lambda **k: SimpleNamespace(action="enter_long"))
    monkeypatch.setattr("quant.live.runtime.execute_intraday_entry", fake_execute)

    runtime._run_intraday_entries("US", datetime(2026, 6, 24, 14, 15, tzinfo=timezone.utc))
    assert captured["position_fraction_pct"] == 25.0
```

追加到 `backend/tests/test_live_state_overlay.py`:

```python
def test_update_strategy_params_syncs_live_params():
    from quant.live.params import LiveParams
    from quant.live.state import LiveGatewayState
    from app.services.state import AppState

    params = LiveParams()
    state = AppState(LiveGatewayState(), params)
    state.update_strategy_params("intraday_macd", {"stop_loss_pct": 2.5})
    assert params.intraday.stop_loss_pct == 2.5
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py tests/test_live_state_overlay.py -q`
Expected: FAIL（`captured["position_fraction_pct"]` KeyError；`AppState()` 不接受第二参数 / `params` 未同步）

- [ ] **Step 3: 实现**

`backend/quant/live/runtime.py`:

1) 顶部 import 追加 `from quant.live.params import LiveParams`。

2) `LiveRuntime.__init__` 增加参数并存:在签名加 `params: LiveParams | None = None`,体内 `self.params = params or LiveParams()`。

3) `_run_intraday_entries` 的 `execute_intraday_entry(...)` 调用追加参数:

```python
                position_fraction_pct=self.params.intraday.position_fraction_pct,
                max_positions=self.params.intraday.max_positions,
                max_daily_loss_pct=self.params.intraday.max_daily_loss_pct,
```

4) `_run_intraday_exits` 首次 `evaluate_intraday_exit_signal(...)` 追加:

```python
                stop_loss_pct=self.params.intraday.stop_loss_pct,
                take_profit_1_pct=self.params.intraday.take_profit_1_pct,
                take_profit_2_pct=self.params.intraday.take_profit_2_pct,
```

5) `_run_portfolio_entries` 的 `execute_portfolio_entry(...)` 追加:

```python
                single_position_cap_pct=self.params.portfolio.single_position_cap_pct,
                target_positions_max=self.params.portfolio.target_positions_max,
                first_entry_fraction_pct=self.params.portfolio.first_entry_fraction_pct,
```

6) `_run_portfolio_exits` 的 `evaluate_trend_exit_signal(...)` 追加:

```python
                max_symbol_drawdown_pct=self.params.portfolio.max_symbol_drawdown_pct,
                take_profit_pct=self.params.portfolio.take_profit_pct,
                rebalance_months=self.params.portfolio.rebalance_months,
```

7) `_observe_account` 用参数的 `max_daily_loss_pct`：把 `self.config.max_daily_loss_pct` 改为 `self.params.intraday.max_daily_loss_pct`。

8) `build_live_runtime_from_env(live_state)` 签名改为 `build_live_runtime_from_env(live_state, params=None)`,并把 `params` 透传给 `LiveRuntime(..., params=params)`。

`backend/app/services/state.py`:`AppState.__init__` 增加 `params: LiveParams | None = None`(import `from quant.live.params import LiveParams`),`self.params = params or LiveParams()`;`update_strategy_params` 末尾(更新展示 dict 后)加 `self.params.update(strategy_id, params)`。

`backend/app/main.py`:`create_app` 内构造共享实例并注入:

```python
    live_state = LiveGatewayState()
    live_params = LiveParams()
    live_runtime = build_live_runtime_from_env(live_state, live_params)
    ...
    app.state.quant_state = AppState(live_state, live_params)
```

(顶部 import `from quant.live.params import LiveParams`。)

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS（全绿，127 + 各 Task 新增用例）

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/runtime.py backend/app/services/state.py backend/app/main.py backend/tests/quant/live/test_runtime.py backend/tests/test_live_state_overlay.py
git commit -m "feat(live): runtime 与 AppState 接入共享实盘参数

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage（对照 spec 第 5 节 Plan 2）**
- 5.1 当日权益基线→3%熔断+闩锁 → Task 1(状态)+ Task 2(接线+risk 拦截) ✓
- 5.2 holding_days→满6月 → Task 4(状态+回放纯函数)+ Task 5(接线+启动回放) ✓
- 5.3 MACD 反向交叉平仓 → Task 3 ✓
- 5.4 做空接线 → Task 6 ✓
- 5.5 参数注入 → Task 7(参数源)+ Task 8(runtime/AppState 接入) ✓
- 5.6 测试(fake 账户/bar/持仓) → 各 Task 的 Step 1 ✓

**2. Placeholder scan:** 无 TBD/TODO；每个代码步骤含完整代码。

**3. Type consistency:**
- `observe_account_equity/daily_loss_pct/trip_halt_if_breached/is_halted`（Task 1)→ Task 2 调用一致。
- `evaluate_live_order_risk(..., account_halted)`（Task 2)→ Task 6 `_live_risk` 透传一致。
- `record_portfolio_entry_date/holding_days/load_entry_dates_from_events` + `entry_dates_from_trade_events`（Task 4)→ Task 5 调用一致。
- `_reverse_cross(symbol, side)`（Task 3)签名与 `_run_intraday_exits` 调用一致。
- `_is_shortable(symbol)`、`evaluate_intraday_entry_signal(side=, shortable=)`、`execute_intraday_entry(is_short=, shortable=)`（Task 6)与既有函数签名一致(见 intraday.py / executor.py)。
- `LiveParams.update`、`IntradayParams`/`PortfolioParams` 字段（Task 7)→ Task 8 读取的属性名逐一对应执行器/策略参数名。

**4. 依赖顺序:** Task 2 依赖 1;Task 5 依赖 4;Task 6 依赖 2(`_live_risk` 已含 `account_halted`)与 Plan 1 的 `shortable`;Task 8 依赖 7。顺序执行即可。

**边界说明:** `_live_risk` 在 Task 2 先扩展 `account_halted`,Task 6 再加 `is_short`——两次修改同一方法,按 Task 顺序叠加。Task 8 给 `execute_*`/`evaluate_*` 追加参数时,保持 Task 2/3/6 已加参数不被覆盖(追加而非替换)。
