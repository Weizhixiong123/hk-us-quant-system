# Plan A · 富途网关层 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接入 vnpy 富途网关,提供连接/行情订阅/账户持仓查询/下单的封装,并把回报写入线程安全的共享状态——作为实盘系统的最底层。

**Architecture:** 切成两层。**纯逻辑层**(`config` / `translate` / `state`)不依赖 vnpy,可远程单测;**vnpy 适配层**(`gateway.py`)真正 import vnpy 与 FutuGateway,只能在用户本地连 FutuOpenD 验证。vnpy 回报(账户/持仓/订单/行情)经纯函数转成内部 Gateway 数据类,写入加锁的共享状态。

**Tech Stack:** Python 3.11、vnpy、vnpy_futu、本地 FutuOpenD;测试用 pytest(远程 venv 无 vnpy)。

## Global Constraints

- Python 3.11,`backend/` 为导入根(测试从 `backend/` 运行)
- **纯逻辑模块(`config.py` / `translate.py` / `state.py`)严禁 import vnpy**;远程测试环境无 vnpy,只有 `gateway.py` 可 import vnpy
- 不修改 `app/` 现有对外行为;现有 `backend/tests/` 必须保持通过
- 先模拟盘:默认 `trd_env="SIMULATE"`
- 单元测试禁止真实连接富途 / FutuOpenD / vnpy
- vnpy 数据结构字段名以**用户本地 vnpy 版本为准**;`translate.py` 用鸭子类型(读属性),字段映射集中一处,Task 4 本地联调时核对
- 测试命令统一:`cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest <path> -v`

---

### Task 1: 网关配置

**Files:**
- Create: `backend/quant/live/__init__.py`
- Create: `backend/quant/live/config.py`
- Create: `backend/tests/quant/live/__init__.py`
- Create: `backend/tests/quant/live/test_config.py`

**Interfaces:**
- Produces:
  - `FutuGatewayConfig` dataclass(frozen): `host: str`, `port: int`, `trd_env: str`, `market: str`, `paper: bool`
  - `load_futu_config() -> FutuGatewayConfig`:从环境变量读取,带默认值,校验 `trd_env ∈ {"SIMULATE","REAL"}`

- [ ] **Step 1: 写失败测试**

`backend/tests/quant/live/test_config.py`:

```python
import pytest
from quant.live.config import FutuGatewayConfig, load_futu_config


def test_defaults(monkeypatch):
    for var in ("FUTU_HOST", "FUTU_PORT", "FUTU_TRD_ENV", "FUTU_MARKET"):
        monkeypatch.delenv(var, raising=False)
    cfg = load_futu_config()
    assert cfg.host == "127.0.0.1"
    assert cfg.port == 11111
    assert cfg.trd_env == "SIMULATE"
    assert cfg.paper is True


def test_env_override(monkeypatch):
    monkeypatch.setenv("FUTU_HOST", "192.168.1.10")
    monkeypatch.setenv("FUTU_PORT", "22222")
    monkeypatch.setenv("FUTU_TRD_ENV", "REAL")
    cfg = load_futu_config()
    assert cfg.host == "192.168.1.10"
    assert cfg.port == 22222
    assert cfg.trd_env == "REAL"
    assert cfg.paper is False


def test_invalid_trd_env_raises(monkeypatch):
    monkeypatch.setenv("FUTU_TRD_ENV", "DEMO")
    with pytest.raises(ValueError):
        load_futu_config()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live/test_config.py -v`
Expected: FAIL（ModuleNotFoundError: quant.live.config）

- [ ] **Step 3: 实现**

`backend/quant/live/__init__.py`、`backend/tests/quant/live/__init__.py`：空文件。

`backend/quant/live/config.py`:

```python
from __future__ import annotations

import os
from dataclasses import dataclass

_VALID_TRD_ENV = {"SIMULATE", "REAL"}


@dataclass(frozen=True)
class FutuGatewayConfig:
    host: str
    port: int
    trd_env: str
    market: str
    paper: bool


def load_futu_config() -> FutuGatewayConfig:
    trd_env = os.getenv("FUTU_TRD_ENV", "SIMULATE").upper()
    if trd_env not in _VALID_TRD_ENV:
        raise ValueError(f"FUTU_TRD_ENV must be one of {_VALID_TRD_ENV}, got {trd_env}")
    return FutuGatewayConfig(
        host=os.getenv("FUTU_HOST", "127.0.0.1"),
        port=int(os.getenv("FUTU_PORT", "11111")),
        trd_env=trd_env,
        market=os.getenv("FUTU_MARKET", "HK"),
        paper=trd_env == "SIMULATE",
    )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live/test_config.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/__init__.py backend/quant/live/config.py backend/tests/quant/live/__init__.py backend/tests/quant/live/test_config.py
git commit -m "feat(live): 富途网关配置"
```

---

### Task 2: 网关数据类 + vnpy 回报转换(纯函数)

**Files:**
- Create: `backend/quant/live/translate.py`
- Create: `backend/tests/quant/live/test_translate.py`

**Interfaces:**
- Produces(frozen dataclasses):
  - `GatewayAccount`: `account_id: str`, `balance: float`, `available: float`, `frozen: float`
  - `GatewayPosition`: `symbol: str`, `direction: str`, `volume: float`, `price: float`, `pnl: float`
  - `GatewayOrder`: `order_id: str`, `symbol: str`, `direction: str`, `offset: str`, `price: float`, `volume: float`, `traded: float`, `status: str`
  - `GatewayTick`: `symbol: str`, `last_price: float`, `volume: float`, `time: str`
  - 转换函数(入参为鸭子类型 vnpy 对象,**不 import vnpy**):
    `account_from_vnpy(obj) -> GatewayAccount`、`position_from_vnpy(obj) -> GatewayPosition`、`order_from_vnpy(obj) -> GatewayOrder`、`tick_from_vnpy(obj) -> GatewayTick`
  - 辅助 `enum_value(x) -> str`:若对象有 `.value` 取之并转 str,否则 `str(x)`

**说明:** vnpy 字段约定(本地核对):AccountData=`accountid/balance/frozen`(available 为 balance-frozen);PositionData=`symbol/direction/volume/price/pnl`;OrderData=`orderid/symbol/direction/offset/price/volume/traded/status/datetime`;TickData=`symbol/last_price/volume/datetime`。direction/offset/status 为枚举,用 `enum_value` 取字符串。

- [ ] **Step 1: 写失败测试**

`backend/tests/quant/live/test_translate.py`:

```python
from types import SimpleNamespace
from quant.live.translate import (
    GatewayAccount, GatewayOrder, GatewayPosition, GatewayTick,
    account_from_vnpy, order_from_vnpy, position_from_vnpy, tick_from_vnpy,
)


class _Enum:
    def __init__(self, value):
        self.value = value


def test_account_from_vnpy_computes_available():
    obj = SimpleNamespace(accountid="ACC1", balance=100000.0, frozen=20000.0)
    acc = account_from_vnpy(obj)
    assert acc == GatewayAccount(account_id="ACC1", balance=100000.0, available=80000.0, frozen=20000.0)


def test_position_from_vnpy_maps_enum_direction():
    obj = SimpleNamespace(symbol="AAPL", direction=_Enum("多"), volume=400, price=198.4, pnl=1080.0)
    pos = position_from_vnpy(obj)
    assert pos == GatewayPosition(symbol="AAPL", direction="多", volume=400, price=198.4, pnl=1080.0)


def test_order_from_vnpy():
    obj = SimpleNamespace(
        orderid="ORD1", symbol="AAPL", direction=_Enum("多"), offset=_Enum("开"),
        price=198.4, volume=400, traded=0, status=_Enum("提交中"),
    )
    o = order_from_vnpy(obj)
    assert o.order_id == "ORD1" and o.status == "提交中" and o.offset == "开"


def test_tick_from_vnpy_formats_time():
    obj = SimpleNamespace(symbol="AAPL", last_price=201.1, volume=1000,
                          datetime=SimpleNamespace(isoformat=lambda: "2026-06-22T10:00:00"))
    t = tick_from_vnpy(obj)
    assert t == GatewayTick(symbol="AAPL", last_price=201.1, volume=1000, time="2026-06-22T10:00:00")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live/test_translate.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

`backend/quant/live/translate.py`:

```python
from __future__ import annotations

from dataclasses import dataclass


def enum_value(x: object) -> str:
    value = getattr(x, "value", x)
    return str(value)


def _time_str(value: object) -> str:
    iso = getattr(value, "isoformat", None)
    return iso() if callable(iso) else str(value)


@dataclass(frozen=True)
class GatewayAccount:
    account_id: str
    balance: float
    available: float
    frozen: float


@dataclass(frozen=True)
class GatewayPosition:
    symbol: str
    direction: str
    volume: float
    price: float
    pnl: float


@dataclass(frozen=True)
class GatewayOrder:
    order_id: str
    symbol: str
    direction: str
    offset: str
    price: float
    volume: float
    traded: float
    status: str


@dataclass(frozen=True)
class GatewayTick:
    symbol: str
    last_price: float
    volume: float
    time: str


def account_from_vnpy(obj: object) -> GatewayAccount:
    balance = float(getattr(obj, "balance", 0.0))
    frozen = float(getattr(obj, "frozen", 0.0))
    return GatewayAccount(
        account_id=str(getattr(obj, "accountid", "")),
        balance=balance,
        available=balance - frozen,
        frozen=frozen,
    )


def position_from_vnpy(obj: object) -> GatewayPosition:
    return GatewayPosition(
        symbol=str(getattr(obj, "symbol", "")),
        direction=enum_value(getattr(obj, "direction", "")),
        volume=float(getattr(obj, "volume", 0.0)),
        price=float(getattr(obj, "price", 0.0)),
        pnl=float(getattr(obj, "pnl", 0.0)),
    )


def order_from_vnpy(obj: object) -> GatewayOrder:
    return GatewayOrder(
        order_id=str(getattr(obj, "orderid", "")),
        symbol=str(getattr(obj, "symbol", "")),
        direction=enum_value(getattr(obj, "direction", "")),
        offset=enum_value(getattr(obj, "offset", "")),
        price=float(getattr(obj, "price", 0.0)),
        volume=float(getattr(obj, "volume", 0.0)),
        traded=float(getattr(obj, "traded", 0.0)),
        status=enum_value(getattr(obj, "status", "")),
    )


def tick_from_vnpy(obj: object) -> GatewayTick:
    return GatewayTick(
        symbol=str(getattr(obj, "symbol", "")),
        last_price=float(getattr(obj, "last_price", 0.0)),
        volume=float(getattr(obj, "volume", 0.0)),
        time=_time_str(getattr(obj, "datetime", "")),
    )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live/test_translate.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/translate.py backend/tests/quant/live/test_translate.py
git commit -m "feat(live): vnpy回报转内部网关数据类"
```

---

### Task 3: 线程安全的网关共享状态

**Files:**
- Create: `backend/quant/live/state.py`
- Create: `backend/tests/quant/live/test_state.py`

**Interfaces:**
- Consumes: `GatewayAccount`/`GatewayPosition`/`GatewayOrder`/`GatewayTick`(Task 2)
- Produces:
  - `LiveGatewayState` 类(内部 `threading.RLock`):
    - `set_connected(connected: bool, detail: str = "") -> None`
    - `is_connected() -> bool`
    - `update_account(acc: GatewayAccount) -> None`
    - `update_position(pos: GatewayPosition) -> None`(按 symbol+direction 覆盖;volume==0 则移除)
    - `update_order(order: GatewayOrder) -> None`(按 order_id 覆盖)
    - `update_tick(tick: GatewayTick) -> None`(按 symbol 覆盖)
    - `snapshot() -> dict`:返回 `{connected, detail, account, positions, orders, ticks}`(positions/orders/ticks 为 list)

- [ ] **Step 1: 写失败测试**

`backend/tests/quant/live/test_state.py`:

```python
from quant.live.state import LiveGatewayState
from quant.live.translate import GatewayAccount, GatewayOrder, GatewayPosition, GatewayTick


def test_connection_flag():
    s = LiveGatewayState()
    assert s.is_connected() is False
    s.set_connected(True, "ok")
    assert s.is_connected() is True
    assert s.snapshot()["detail"] == "ok"


def test_position_upsert_and_remove_on_zero():
    s = LiveGatewayState()
    s.update_position(GatewayPosition("AAPL", "多", 400, 198.4, 0.0))
    s.update_position(GatewayPosition("AAPL", "多", 500, 199.0, 10.0))  # 覆盖
    assert len(s.snapshot()["positions"]) == 1
    assert s.snapshot()["positions"][0].volume == 500
    s.update_position(GatewayPosition("AAPL", "多", 0, 0.0, 0.0))  # 清零移除
    assert s.snapshot()["positions"] == []


def test_order_and_tick_upsert():
    s = LiveGatewayState()
    s.update_order(GatewayOrder("O1", "AAPL", "多", "开", 198.4, 400, 0, "提交中"))
    s.update_order(GatewayOrder("O1", "AAPL", "多", "开", 198.4, 400, 400, "全部成交"))
    assert len(s.snapshot()["orders"]) == 1
    assert s.snapshot()["orders"][0].status == "全部成交"
    s.update_tick(GatewayTick("AAPL", 201.1, 1000, "t1"))
    s.update_tick(GatewayTick("AAPL", 202.0, 1100, "t2"))
    assert len(s.snapshot()["ticks"]) == 1
    assert s.snapshot()["ticks"][0].last_price == 202.0


def test_account_update():
    s = LiveGatewayState()
    s.update_account(GatewayAccount("ACC1", 100000.0, 80000.0, 20000.0))
    assert s.snapshot()["account"].available == 80000.0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live/test_state.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

`backend/quant/live/state.py`:

```python
from __future__ import annotations

from threading import RLock

from quant.live.translate import (
    GatewayAccount,
    GatewayOrder,
    GatewayPosition,
    GatewayTick,
)


class LiveGatewayState:
    def __init__(self) -> None:
        self._lock = RLock()
        self._connected = False
        self._detail = ""
        self._account: GatewayAccount | None = None
        self._positions: dict[str, GatewayPosition] = {}
        self._orders: dict[str, GatewayOrder] = {}
        self._ticks: dict[str, GatewayTick] = {}

    def set_connected(self, connected: bool, detail: str = "") -> None:
        with self._lock:
            self._connected = connected
            self._detail = detail

    def is_connected(self) -> bool:
        with self._lock:
            return self._connected

    def update_account(self, acc: GatewayAccount) -> None:
        with self._lock:
            self._account = acc

    def update_position(self, pos: GatewayPosition) -> None:
        key = f"{pos.symbol}:{pos.direction}"
        with self._lock:
            if pos.volume == 0:
                self._positions.pop(key, None)
            else:
                self._positions[key] = pos

    def update_order(self, order: GatewayOrder) -> None:
        with self._lock:
            self._orders[order.order_id] = order

    def update_tick(self, tick: GatewayTick) -> None:
        with self._lock:
            self._ticks[tick.symbol] = tick

    def snapshot(self) -> dict:
        with self._lock:
            return {
                "connected": self._connected,
                "detail": self._detail,
                "account": self._account,
                "positions": list(self._positions.values()),
                "orders": list(self._orders.values()),
                "ticks": list(self._ticks.values()),
            }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live/test_state.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/state.py backend/tests/quant/live/test_state.py
git commit -m "feat(live): 线程安全网关共享状态"
```

---

### Task 4: vnpy 富途网关适配器(本地验证,非远程 TDD)

> **重要:** 本任务真正 import vnpy 与 vnpy_futu,**只能在用户本地连 FutuOpenD 验证**,远程测试环境无 vnpy。因此本任务不写 pytest 单测,改为「实现 + 本地验证清单」。实现者(或用户)在本地按清单逐项确认。

**Files:**
- Create: `backend/quant/live/gateway.py`
- Modify: `backend/requirements.txt`(追加 `vnpy>=3.9.0`、`vnpy_futu>=1.0.0`,版本以本地可装为准)
- Create: `backend/docs/futu-gateway-local-verify.md`(本地验证清单)

**Interfaces:**
- Consumes: `FutuGatewayConfig`(Task 1)、`*_from_vnpy` 转换函数(Task 2)、`LiveGatewayState`(Task 3)
- Produces:
  - `FutuLiveGateway` 类:
    - `__init__(self, config: FutuGatewayConfig, state: LiveGatewayState)`
    - `connect() -> None`:创建 `EventEngine`/`MainEngine`,`add_gateway(FutuGateway)`,注册事件回调(EVENT_ACCOUNT/EVENT_POSITION/EVENT_ORDER/EVENT_TRADE/EVENT_TICK → 经 translate 写入 state),按 config 连接
    - `subscribe(symbols: list[str]) -> None`
    - `send_order(symbol, direction, offset, price, volume) -> str`:构造 vnpy `OrderRequest` 并 `main_engine.send_order`,返回 vt_orderid
    - `close() -> None`

- [ ] **Step 1: 实现网关适配器**

`backend/quant/live/gateway.py`:

```python
from __future__ import annotations

from quant.live.config import FutuGatewayConfig
from quant.live.state import LiveGatewayState
from quant.live.translate import (
    account_from_vnpy,
    order_from_vnpy,
    position_from_vnpy,
    tick_from_vnpy,
)


class FutuLiveGateway:
    """vnpy 富途网关封装。仅本地(连 FutuOpenD)可运行。

    字段映射依赖 quant.live.translate;若本地 vnpy 版本字段名不同,
    在 translate.py 的 *_from_vnpy 集中调整。
    """

    def __init__(self, config: FutuGatewayConfig, state: LiveGatewayState) -> None:
        self.config = config
        self.state = state
        self._main_engine = None
        self._event_engine = None

    def connect(self) -> None:
        # 延迟 import:远程环境无 vnpy,导入只在本地真正调用时发生
        from vnpy.event import EventEngine
        from vnpy.trader.engine import MainEngine
        from vnpy.trader.event import (
            EVENT_ACCOUNT,
            EVENT_ORDER,
            EVENT_POSITION,
            EVENT_TICK,
            EVENT_TRADE,
        )
        from vnpy_futu import FutuGateway

        self._event_engine = EventEngine()
        self._main_engine = MainEngine(self._event_engine)
        self._main_engine.add_gateway(FutuGateway)

        self._event_engine.register(EVENT_ACCOUNT, self._on_account)
        self._event_engine.register(EVENT_POSITION, self._on_position)
        self._event_engine.register(EVENT_ORDER, self._on_order)
        self._event_engine.register(EVENT_TRADE, self._on_trade)
        self._event_engine.register(EVENT_TICK, self._on_tick)

        setting = {
            "市场": self.config.market,
            "host": self.config.host,
            "port": self.config.port,
            "trd_env": self.config.trd_env,
        }
        self._main_engine.connect(setting, "FUTU")
        self.state.set_connected(True, f"FUTU {self.config.trd_env} 已连接")

    def subscribe(self, symbols: list[str]) -> None:
        from vnpy.trader.object import SubscribeRequest
        from vnpy.trader.constant import Exchange

        for symbol in symbols:
            req = SubscribeRequest(symbol=symbol, exchange=Exchange.SEHK)
            self._main_engine.subscribe(req, "FUTU")

    def send_order(self, symbol, direction, offset, price, volume) -> str:
        from vnpy.trader.object import OrderRequest
        from vnpy.trader.constant import Direction, Exchange, Offset, OrderType

        req = OrderRequest(
            symbol=symbol,
            exchange=Exchange.SEHK,
            direction=Direction(direction),
            type=OrderType.LIMIT,
            volume=volume,
            price=price,
            offset=Offset(offset),
        )
        return self._main_engine.send_order(req, "FUTU")

    def close(self) -> None:
        if self._main_engine is not None:
            self._main_engine.close()
        self.state.set_connected(False, "已断开")

    def _on_account(self, event) -> None:
        self.state.update_account(account_from_vnpy(event.data))

    def _on_position(self, event) -> None:
        self.state.update_position(position_from_vnpy(event.data))

    def _on_order(self, event) -> None:
        self.state.update_order(order_from_vnpy(event.data))

    def _on_trade(self, event) -> None:
        # 成交回报暂记录订单维度;持仓由 EVENT_POSITION 维护
        self.state.update_order(order_from_vnpy(event.data)) if hasattr(event.data, "orderid") else None

    def _on_tick(self, event) -> None:
        self.state.update_tick(tick_from_vnpy(event.data))
```

> 注:`Exchange.SEHK` 为占位,美股应为 `Exchange.NASDAQ`/`SMART` 等;多市场映射在本地联调时按 vnpy_futu 实际要求补全(记入开放项,Plan C/D 用到再细化)。

- [ ] **Step 2: 追加依赖**

`backend/requirements.txt` 末尾追加:

```
vnpy>=3.9.0
vnpy_futu>=1.0.0
```

- [ ] **Step 3: 写本地验证清单**

`backend/docs/futu-gateway-local-verify.md`:

```markdown
# 富途网关本地验证清单

前置:已装 vnpy + vnpy_futu;FutuOpenD 已登录并开通模拟交易;`get_acc_list()` 能返回 SIMULATE 账户。

环境变量(模拟盘):
- FUTU_HOST=127.0.0.1  FUTU_PORT=11111  FUTU_TRD_ENV=SIMULATE  FUTU_MARKET=HK

逐项确认:
- [ ] `FutuLiveGateway(config, state).connect()` 不抛异常,`state.is_connected()` 为 True
- [ ] 连接后短时内收到 EVENT_ACCOUNT,`state.snapshot()["account"]` 非空且 balance 合理
- [ ] `subscribe(["00700"])` 后收到 EVENT_TICK,`state.snapshot()["ticks"]` 有 00700 且 last_price>0
- [ ] `send_order("00700","多","开",<现价>,100)` 返回 vt_orderid;收到 EVENT_ORDER,state.orders 出现该单
- [ ] 若有持仓,EVENT_POSITION 使 state.positions 正确(volume 清零时移除)
- [ ] translate.py 的字段假设与真实 vnpy 数据一致(如不一致,在 translate.py 调整并补单测)
- [ ] `close()` 后 `state.is_connected()` 为 False
```

- [ ] **Step 4: 远程冒烟(仅确认纯逻辑不被破坏 + gateway 模块不在导入期触发 vnpy)**

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -c "import ast; ast.parse(open('quant/live/gateway.py').read()); print('syntax ok')"`
Expected: 输出 `syntax ok`(确认语法正确;不 import 执行,因无 vnpy)

Run: `cd /www/hk-us-quant-system/backend && ./.venv/bin/python -m pytest tests/quant/live -v`
Expected: PASS（Task 1-3 共 11 passed,gateway 未被导入)

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/gateway.py backend/requirements.txt backend/docs/futu-gateway-local-verify.md
git commit -m "feat(live): vnpy富途网关适配器(本地验证)"
```

---

## 自检结论(对照 spec §2/§3)

- spec §3 网关封装 → Task 4(`gateway.py`);交易时钟/调度/策略/执行器/Web 属 Plan B~E,本计划不含
- spec §2 vnpy 回调写线程安全共享状态 → Task 2(转换)+ Task 3(加锁 state)+ Task 4(回调注册)
- spec §1.3 纯逻辑可远程测、网关本地验证 → Task 1-3 远程 TDD;Task 4 本地清单 + 远程语法冒烟
- Global Constraint「纯逻辑模块不 import vnpy」→ config/translate/state 均无 vnpy 依赖;gateway.py 用延迟 import,导入模块不触发 vnpy

类型一致性:`GatewayAccount/Position/Order/Tick` 字段、`*_from_vnpy`、`LiveGatewayState` 方法签名跨任务一致。无占位符(`Exchange.SEHK`/多市场映射为已标注的本地联调待细化项,非代码占位)。
