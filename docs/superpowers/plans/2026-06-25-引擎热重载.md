# 引擎热重载(单租户网页自助)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 客户在网页保存配置后点「保存并应用」,引擎进程内热重载、即时生效,无需 SSH 重启服务器。

**Architecture:** 新增 `RuntimeManager` 封装引擎生命周期(start/stop/reload),持有跨重载保留的 `live_state`/`params`;`main.py` 改为持有 manager;新增 `POST /api/runtime/reload` 触发重载;配置页加「保存并应用」按钮(保存 → 重载)。

**Tech Stack:** Python 3.11 / asyncio / FastAPI / pytest;前端 Vue 3 + TS。

## Global Constraints

- 纯逻辑模块(含 `RuntimeManager`)严禁 import vnpy / fastapi;远程用 `DryRunGateway`/fake 单测。
- 测试运行:`cd backend && ./.venv/bin/python -m pytest -q`(导入根 `backend/`)。
- 远程 venv 无 fastapi:依赖 fastapi 的测试(`tests/test_live_settings_api.py`)只能本地跑;计划中标注。
- reload 失败 = 引擎置 `None`(已停止安全态),不回滚。
- 提交信息中文,结尾带 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

### Task 1: RuntimeManager 生命周期与热重载

**Files:**
- Create: `backend/quant/live/runtime_manager.py`
- Test: `backend/tests/quant/live/test_runtime_manager.py`

**Interfaces:**
- Consumes: `build_live_runtime_from_env(live_state, params) -> LiveRuntime`(已存在,`quant/live/runtime.py`);`LiveRuntime.start()`/`.stop()` 均为 async。
- Produces:
  - `RuntimeManager(live_state, params, build=build_live_runtime_from_env)`
  - `RuntimeManager.runtime -> LiveRuntime | None`(当前引擎,失败/未启为 None)
  - `RuntimeManager.last_error -> str | None`
  - `async RuntimeManager.start() -> None`
  - `async RuntimeManager.stop() -> None`
  - `async RuntimeManager.reload() -> dict`(返回 `{"ok": bool, "error": str | None}`)

- [ ] **Step 1: 写失败测试 — reload 用最新配置重建并启动**

`backend/tests/quant/live/test_runtime_manager.py`:

```python
from __future__ import annotations

import asyncio

from quant.live.runtime_manager import RuntimeManager


class FakeRuntime:
    def __init__(self, tag: str, fail_on_start: bool = False) -> None:
        self.tag = tag
        self.fail_on_start = fail_on_start
        self.started = False
        self.stopped = False

    async def start(self) -> None:
        if self.fail_on_start:
            raise RuntimeError("网关连接失败")
        self.started = True

    async def stop(self) -> None:
        self.stopped = True


def test_reload_rebuilds_and_starts_new_runtime():
    builds: list[FakeRuntime] = []

    def build(live_state, params):
        rt = FakeRuntime(f"rt{len(builds)}")
        builds.append(rt)
        return rt

    manager = RuntimeManager(live_state=object(), params=object(), build=build)

    async def scenario():
        await manager.start()
        first = manager.runtime
        result = await manager.reload()
        return first, result

    first, result = asyncio.run(scenario())

    assert result == {"ok": True, "error": None}
    assert first.stopped is True            # 旧引擎已停
    assert manager.runtime is builds[-1]    # 当前是新建实例
    assert manager.runtime.started is True  # 新引擎已启动
    assert manager.runtime is not first
```

- [ ] **Step 2: 运行,确认失败**

Run: `cd backend && ./.venv/bin/python -m pytest tests/quant/live/test_runtime_manager.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'quant.live.runtime_manager'`

- [ ] **Step 3: 写最小实现**

`backend/quant/live/runtime_manager.py`:

```python
from __future__ import annotations

import asyncio
from typing import Any, Callable

from quant.live.runtime import build_live_runtime_from_env


class RuntimeManager:
    def __init__(
        self,
        live_state: Any,
        params: Any,
        build: Callable[[Any, Any], Any] = build_live_runtime_from_env,
    ) -> None:
        self._live_state = live_state
        self._params = params
        self._build = build
        self._runtime: Any | None = None
        self._lock = asyncio.Lock()
        self.last_error: str | None = None

    @property
    def runtime(self) -> Any | None:
        return self._runtime

    async def start(self) -> None:
        async with self._lock:
            await self._build_and_start()

    async def stop(self) -> None:
        async with self._lock:
            if self._runtime is not None:
                await self._runtime.stop()
            self._runtime = None

    async def reload(self) -> dict:
        async with self._lock:
            if self._runtime is not None:
                await self._runtime.stop()
                self._runtime = None
            try:
                await self._build_and_start()
                self.last_error = None
                return {"ok": True, "error": None}
            except Exception as exc:  # 失败置停止态,不回滚
                self._runtime = None
                self.last_error = str(exc)
                return {"ok": False, "error": str(exc)}

    async def _build_and_start(self) -> None:
        runtime = self._build(self._live_state, self._params)
        await runtime.start()
        self._runtime = runtime
```

- [ ] **Step 4: 运行,确认通过**

Run: `cd backend && ./.venv/bin/python -m pytest tests/quant/live/test_runtime_manager.py -q`
Expected: PASS

- [ ] **Step 5: 写失败测试 — reload 失败置停止态并返回错误**

追加到测试文件:

```python
def test_reload_failure_stops_engine_and_reports_error():
    calls = {"n": 0}

    def build(live_state, params):
        calls["n"] += 1
        # 第一次(start)成功,第二次(reload)启动失败
        return FakeRuntime("rt", fail_on_start=calls["n"] >= 2)

    manager = RuntimeManager(live_state=object(), params=object(), build=build)

    async def scenario():
        await manager.start()
        return await manager.reload()

    result = asyncio.run(scenario())

    assert result["ok"] is False
    assert "网关连接失败" in result["error"]
    assert manager.runtime is None          # 安全停止态
    assert manager.last_error == "网关连接失败"
```

- [ ] **Step 6: 运行,确认通过(实现已覆盖)**

Run: `cd backend && ./.venv/bin/python -m pytest tests/quant/live/test_runtime_manager.py -q`
Expected: PASS(两个测试)。若 FAIL,修实现勿改测试。

- [ ] **Step 7: 写失败测试 — 并发 reload 被串行化**

追加:

```python
def test_concurrent_reloads_are_serialized():
    active = {"count": 0, "max": 0}

    class SlowRuntime(FakeRuntime):
        async def start(self) -> None:
            active["count"] += 1
            active["max"] = max(active["max"], active["count"])
            await asyncio.sleep(0.01)
            active["count"] -= 1
            self.started = True

    def build(live_state, params):
        return SlowRuntime("rt")

    manager = RuntimeManager(live_state=object(), params=object(), build=build)

    async def scenario():
        await manager.start()
        await asyncio.gather(manager.reload(), manager.reload(), manager.reload())

    asyncio.run(scenario())

    assert active["max"] == 1  # 任意时刻只有一个 reload 在跑
```

- [ ] **Step 8: 运行,确认通过**

Run: `cd backend && ./.venv/bin/python -m pytest tests/quant/live/test_runtime_manager.py -q`
Expected: PASS(三个测试)

- [ ] **Step 9: 全套回归 + 提交**

```bash
cd backend && ./.venv/bin/python -m pytest --ignore=tests/test_live_settings_api.py -q
git add backend/quant/live/runtime_manager.py backend/tests/quant/live/test_runtime_manager.py
git commit -m "$(printf 'feat(live): RuntimeManager 支持引擎热重载\n\nstart/stop/reload + asyncio.Lock 串行化,reload 失败置停止态不回滚。\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```
Expected: 全绿(原 170 + 3 新)。

---

### Task 2: main.py 接入 RuntimeManager,health 经 manager 取状态

**Files:**
- Modify: `backend/app/main.py:16-37`
- Modify: `backend/app/api/routes.py:33-43`(health)

**Interfaces:**
- Consumes: `RuntimeManager`(Task 1)。
- Produces: `app.state.runtime_manager: RuntimeManager`;health 字段 `runtime_running`(bool)。

> 依赖 fastapi,远程 venv 无法自动化测试;改动小,本地 `uvicorn` 启动 + `GET /api/health` 验证。

- [ ] **Step 1: 改 main.py 用 RuntimeManager**

将 `backend/app/main.py:16-37` 改为:

```python
def create_app() -> FastAPI:
    live_state = LiveGatewayState()
    live_params = LiveParams()
    runtime_manager = RuntimeManager(live_state, live_params)

    @asynccontextmanager
    async def lifespan(app: FastAPI):
        await app.state.runtime_manager.start()
        try:
            yield
        finally:
            await app.state.runtime_manager.stop()

    app = FastAPI(
        title=settings.app_name,
        version=settings.api_version,
        description="FastAPI service for the HK/US dual-strategy quant system.",
        lifespan=lifespan,
    )
    app.state.live_state = live_state
    app.state.runtime_manager = runtime_manager
    app.state.quant_state = AppState(app.state.live_state, live_params)
```

并把 import 行 `from quant.live.runtime import build_live_runtime_from_env` 改为
`from quant.live.runtime_manager import RuntimeManager`。

- [ ] **Step 2: 改 health 经 manager 取 runtime**

将 `backend/app/api/routes.py:34-43` 的 `health` 改为:

```python
@router.get("/health")
def health(request: Request) -> dict[str, str]:
    manager = getattr(request.app.state, "runtime_manager", None)
    runtime = manager.runtime if manager else None
    return {
        "status": "ok",
        "service": request.app.title,
        "mode": "live-ready",
        "runtime_running": str(runtime is not None).lower(),
        "runtime_enabled": str(bool(runtime and runtime.config.enabled)).lower(),
        "runtime_dry_run": str(bool(runtime and runtime.config.dry_run)).lower(),
        "runtime_broker": runtime.config.broker if runtime else "futu",
        "runtime_error": (manager.last_error or "") if manager else "",
    }
```

- [ ] **Step 3: 本地烟测**

```bash
cd backend
set LIVE_RUNTIME_ENABLED=1
set LIVE_RUNTIME_DRY_RUN=1
uvicorn app.main:app --port 8000
# 另开终端:curl http://127.0.0.1:8000/api/health
```
Expected: `runtime_running=true`、`runtime_enabled=true`、`runtime_dry_run=true`。

- [ ] **Step 4: 提交**

```bash
git add backend/app/main.py backend/app/api/routes.py
git commit -m "$(printf 'feat(live): main 接入 RuntimeManager,health 暴露引擎运行态\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 3: POST /api/runtime/reload 接口

**Files:**
- Modify: `backend/app/models/schemas.py`(末尾追加)
- Modify: `backend/app/api/routes.py`(新增路由)
- Test: `backend/tests/test_live_settings_api.py`(追加,本地跑)

**Interfaces:**
- Consumes: `app.state.runtime_manager.reload()`(Task 1/2)。
- Produces: `RuntimeReloadResult` schema;`POST /api/runtime/reload`。

> reload 是 async 且依赖 fastapi,远程无法自动化测试;测试代码写好,本地跑。

- [ ] **Step 1: 加响应 schema**

`backend/app/models/schemas.py` 末尾追加:

```python
class RuntimeReloadResult(BaseModel):
    ok: bool
    error: str | None = None
    runtime_running: bool
    runtime_enabled: bool
    runtime_dry_run: bool
    runtime_broker: str
```

- [ ] **Step 2: 加路由**

`backend/app/api/routes.py` import 处加 `RuntimeReloadResult`;并在 `update_live_settings` 之后追加:

```python
@router.post("/runtime/reload", response_model=RuntimeReloadResult)
async def reload_runtime(request: Request) -> dict:
    manager = getattr(request.app.state, "runtime_manager", None)
    if manager is None:
        raise HTTPException(status_code=503, detail="runtime manager 未初始化")
    result = await manager.reload()
    runtime = manager.runtime
    return {
        "ok": result["ok"],
        "error": result["error"],
        "runtime_running": runtime is not None,
        "runtime_enabled": bool(runtime and runtime.config.enabled),
        "runtime_dry_run": bool(runtime and runtime.config.dry_run),
        "runtime_broker": runtime.config.broker if runtime else "futu",
    }
```

- [ ] **Step 3: 写测试(本地跑)**

`backend/tests/test_live_settings_api.py` 追加:

```python
def test_runtime_reload_returns_running_state():
    from fastapi.testclient import TestClient
    from app.main import create_app

    monkey_env = {"LIVE_RUNTIME_ENABLED": "1", "LIVE_RUNTIME_DRY_RUN": "1"}
    import os

    for k, v in monkey_env.items():
        os.environ[k] = v
    try:
        app = create_app()
        with TestClient(app) as client:
            resp = client.post("/api/runtime/reload")
            assert resp.status_code == 200
            body = resp.json()
            assert body["ok"] is True
            assert body["runtime_running"] is True
            assert body["runtime_dry_run"] is True
    finally:
        for k in monkey_env:
            os.environ.pop(k, None)
```

- [ ] **Step 4: 本地运行测试**

Run(本地有 fastapi): `cd backend && pytest tests/test_live_settings_api.py -q`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add backend/app/models/schemas.py backend/app/api/routes.py backend/tests/test_live_settings_api.py
git commit -m "$(printf 'feat(api): POST /api/runtime/reload 触发引擎热重载\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

### Task 4: 配置页「保存并应用」按钮

**Files:**
- Modify: `frontend/src/api/types.ts`
- Modify: `frontend/src/api/client.ts`
- Modify: `frontend/src/components/LiveSettingsPanel.vue`

**Interfaces:**
- Consumes: `POST /api/runtime/reload`(Task 3)。
- Produces: `client.reloadRuntime(): Promise<RuntimeReloadResult>`。

> 前端验证用 `npx vue-tsc --noEmit`(远程已装依赖)。

- [ ] **Step 1: 加类型**

`frontend/src/api/types.ts` 追加:

```typescript
export interface RuntimeReloadResult {
  ok: boolean;
  error: string | null;
  runtime_running: boolean;
  runtime_enabled: boolean;
  runtime_dry_run: boolean;
  runtime_broker: string;
}
```

- [ ] **Step 2: 加 client 方法**

`frontend/src/api/client.ts` 追加(仿现有 `request` 模式,POST 无 body):

```typescript
export async function reloadRuntime(): Promise<RuntimeReloadResult> {
  return request<RuntimeReloadResult>("/api/runtime/reload", { method: "POST" });
}
```

> 实施时先 `Read` client.ts 确认 `request` 辅助函数签名与 import 写法,保持一致;`RuntimeReloadResult` 加入 import。

- [ ] **Step 3: 配置页加「保存并应用」**

在 `LiveSettingsPanel.vue` 的保存逻辑旁新增方法(保存成功后调用 `reloadRuntime`),并在模板保存按钮旁加一个「保存并应用」按钮。状态:`applying`(loading)、成功提示新状态、失败显示 `result.error`。

> 实施时先 `Read` LiveSettingsPanel.vue 的现有保存函数(`save`/`onSubmit`)与按钮区,复用其表单提交与提示组件;新增 `saveAndApply()`:`await save()` → `const r = await reloadRuntime()` → 据 `r.ok` 提示。

- [ ] **Step 4: 类型检查**

Run: `cd frontend && npx vue-tsc --noEmit`
Expected: 退出码 0,无类型错误。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/api/types.ts frontend/src/api/client.ts frontend/src/components/LiveSettingsPanel.vue
git commit -m "$(printf 'feat(web): 实盘配置页「保存并应用」触发引擎热重载\n\nCo-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>')"
```

---

## Self-Review

**Spec coverage:**
- RuntimeManager.reload(停旧→重建→启动+锁+失败停止) → Task 1 ✅
- 启停/切模式靠 reload(enabled 决定连不连) → 由 `build_live_runtime_from_env` 现有逻辑保证(reload 重建即读最新 settings),Task 1/3 覆盖 ✅
- POST /api/runtime/reload → Task 3 ✅
- 配置页「保存并应用」+ 状态/失败提示 → Task 4 ✅
- 失败安全停止不回滚 → Task 1 Step 5-6 ✅
- 远程 TDD(DryRunGateway/fake)+ 本地真连验证 → Task 1 远程;Task 2/3 本地;真连本地观察 ✅
- 本期不做认证 → 边界,无任务(符合)✅

**Placeholder scan:** Task 4 Step 2-3 用「先 Read 再改」说明而非写死前端现有结构 —— 因 client.ts/LiveSettingsPanel.vue 现有写法需现场核对,属合理的"核对现有接口"指引,非占位(关键新增代码 reloadRuntime/saveAndApply 逻辑已给出)。

**Type consistency:** `reload()` 返回 `{"ok", "error"}`(Task 1)→ Task 3 路由读 `result["ok"]/["error"]` 一致;`RuntimeReloadResult` 字段在 schema(Task 3)/types.ts(Task 4)一致。
