# Plan 1 — 数据真实化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把策略所需的行情/选股/基本面数据从"写死占位"换成真实来源(富途历史分钟补种、yfinance/akshare 基本面、停牌-除权-公告、可做空名单),让策略一/二在真连富途后能拿到真数据。

**Architecture:** 不改运行时架构。新增两个可注入的数据 adapter(`fundamentals` / `market_info`),给 `BarAggregator` 加历史补种,给网关加历史K线查询,在 `DefaultLiveDataProvider` 与 `LiveRuntime` 里接线。所有外部源走依赖注入,远程用 fake 单测;真实源连通留 Plan 3 本地验证。

**Tech Stack:** Python 3.11, pytest, pandas, yfinance/akshare(已在 requirements), vnpy_futu(仅本地)。

## Global Constraints

- 测试从 `backend/` 目录运行:`.venv/bin/python -m pytest`。
- vnpy / vnpy_futu **只能本地运行**:任何 `import vnpy*` 必须延迟到方法内部;远程单测不得触发该 import。
- 外部数据源(财报/行情/历史K线)一律**依赖注入**,默认走真实实现、测试注入 fake。
- 拿不到数据时**走安全侧**:基本面缺失→市值0/盈利0(选股不通过);已配置的停牌源查不到某标的→按停牌排除;未配置停牌源(开发阶段)→不拦截。
- 现有 104 个测试必须保持全绿,不得回归。
- commit message 用中文,前缀 `feat(quant)`/`feat(live)`,结尾附 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

## File Structure

**Create:**
- `backend/quant/data/fundamentals.py` — 基本面 adapter:`RawFundamentals`、`load_fundamentals()`、`default_fundamentals_source()`
- `backend/quant/live/market_info.py` — `SymbolMarketInfo`、`MarketInfoProvider`(停牌/除权/公告)
- `backend/tests/quant/test_fundamentals.py`
- `backend/tests/quant/live/test_market_info.py`

**Modify:**
- `backend/quant/data/universe.py` — `SymbolInfo` 加 `shortable: bool = False`
- `backend/quant/live/market_data.py` — `BarAggregator.seed_minute_bars()`,`max_bars` 默认 500→1500
- `backend/quant/live/gateway.py` — `query_history_minute()` + 纯helper `_bars_from_vnpy()`
- `backend/quant/live/data_provider.py` — 注入 `fundamentals_source`/`risk_blocklist`/`market_info`,删 `_default_fundamentals`
- `backend/quant/live/runtime.py` — 订阅时历史补种(`_seed_history`)
- 对应测试文件:`test_universe.py`、`test_market_data.py`、`test_gateway.py`、`test_data_provider.py`、`test_runtime.py`

---

### Task 1: universe 加可做空标记

**Files:**
- Modify: `backend/quant/data/universe.py`
- Test: `backend/tests/quant/test_universe.py`

**Interfaces:**
- Produces: `SymbolInfo(symbol, name, market, shortable=False)`;`all_symbols()`/`get_universe()` 返回的对象带 `shortable` 字段。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/test_universe.py`:

```python
def test_symbol_info_defaults_not_shortable():
    from quant.data.universe import SymbolInfo

    assert SymbolInfo("TEST", "Test", "US").shortable is False


def test_universe_has_some_shortable_symbols():
    from quant.data.universe import all_symbols

    assert any(item.shortable for item in all_symbols())
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/test_universe.py -q`
Expected: FAIL（`TypeError: __init__() got unexpected... shortable` 或 `AttributeError`）

- [ ] **Step 3: 实现**

`backend/quant/data/universe.py` 修改 `SymbolInfo` 并给部分标的标记可做空:

```python
@dataclass(frozen=True)
class SymbolInfo:
    symbol: str
    name: str
    market: Market
    shortable: bool = False


_UNIVERSE: dict[str, list[SymbolInfo]] = {
    "US": [
        SymbolInfo("AAPL", "Apple", "US", shortable=True),
        SymbolInfo("MSFT", "Microsoft", "US", shortable=True),
        SymbolInfo("NVDA", "NVIDIA", "US"),
    ],
    "HK": [
        SymbolInfo("0700.HK", "腾讯控股", "HK", shortable=True),
        SymbolInfo("9988.HK", "阿里巴巴-W", "HK"),
        SymbolInfo("3690.HK", "美团-W", "HK"),
    ],
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/test_universe.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/data/universe.py backend/tests/quant/test_universe.py
git commit -m "feat(quant): SymbolInfo 增加可做空标记

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: BarAggregator 历史分钟补种

**Files:**
- Modify: `backend/quant/live/market_data.py`
- Test: `backend/tests/quant/live/test_market_data.py`

**Interfaces:**
- Consumes: `Bar(symbol, start, open, high, low, close, volume)`(已存在)。
- Produces: `BarAggregator(max_bars=1500)`;`BarAggregator.seed_minute_bars(symbol: str, bars: Iterable[Bar]) -> None`。补种后 `interval_bars`/`minute_bars` 可立即返回聚合结果;按 `start` 去重并受 `max_bars` 截断。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_market_data.py`:

```python
def _minute_bar(symbol, minute, price, volume=100.0):
    from datetime import datetime, timezone
    from quant.live.market_data import Bar

    start = datetime(2026, 6, 23, 9, minute, tzinfo=timezone.utc)
    return Bar(symbol=symbol, start=start, open=price, high=price, low=price, close=price, volume=volume)


def test_seed_minute_bars_enables_interval_aggregation():
    from quant.live.market_data import BarAggregator

    agg = BarAggregator()
    agg.seed_minute_bars("AAPL", [_minute_bar("AAPL", m, 100 + m) for m in range(30)])
    assert len(agg.interval_bars("AAPL", 15, limit=10)) == 2


def test_seed_minute_bars_dedupes_by_start():
    from quant.live.market_data import BarAggregator

    agg = BarAggregator()
    agg.seed_minute_bars("AAPL", [_minute_bar("AAPL", 1, 100)])
    agg.seed_minute_bars("AAPL", [_minute_bar("AAPL", 1, 100), _minute_bar("AAPL", 2, 101)])
    assert len(agg.minute_bars("AAPL")) == 2


def test_default_max_bars_raised_to_1500():
    from quant.live.market_data import BarAggregator

    assert BarAggregator().max_bars == 1500
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_market_data.py -q`
Expected: FAIL（`AttributeError: 'BarAggregator' object has no attribute 'seed_minute_bars'`）

- [ ] **Step 3: 实现**

`backend/quant/live/market_data.py`:把 `__init__` 默认 `max_bars` 改为 `1500`,并新增方法:

```python
class BarAggregator:
    def __init__(self, max_bars: int = 1500) -> None:
        ...

    def seed_minute_bars(self, symbol: str, bars: Iterable[Bar]) -> None:
        existing = self._minute_bars.setdefault(symbol, [])
        known_starts = {bar.start for bar in existing}
        merged = list(existing)
        for bar in bars:
            if bar.start in known_starts:
                continue
            known_starts.add(bar.start)
            merged.append(bar)
        merged.sort(key=lambda item: item.start)
        if len(merged) > self.max_bars:
            merged = merged[len(merged) - self.max_bars:]
        self._minute_bars[symbol] = merged
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_market_data.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/market_data.py backend/tests/quant/live/test_market_data.py
git commit -m "feat(live): BarAggregator 支持历史分钟补种

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: 基本面 adapter

**Files:**
- Create: `backend/quant/data/fundamentals.py`
- Test: `backend/tests/quant/test_fundamentals.py`

**Interfaces:**
- Consumes: `FundamentalSnapshot(positive_profit_quarters, market_cap, has_major_risk)`（来自 `app.strategies.trend_portfolio`）。
- Produces:
  - `RawFundamentals(market_cap: float, positive_profit_quarters: int)`
  - `FundamentalsSource = Callable[[str, str], RawFundamentals | None]`
  - `load_fundamentals(symbol, market, source, risk_blocklist=(), cache=None) -> FundamentalSnapshot`
  - `default_fundamentals_source(symbol, market) -> RawFundamentals | None`（网络实现,正确性留 Plan 3 验证）

- [ ] **Step 1: 写失败测试**

`backend/tests/quant/test_fundamentals.py`:

```python
from __future__ import annotations

from quant.data.fundamentals import RawFundamentals, load_fundamentals


def test_load_fundamentals_uses_source_values():
    snap = load_fundamentals("AAPL", "US", lambda s, m: RawFundamentals(8e9, 3))
    assert snap.market_cap == 8e9
    assert snap.positive_profit_quarters == 3
    assert snap.has_major_risk is False


def test_blocklist_sets_major_risk():
    snap = load_fundamentals("evil", "US", lambda s, m: RawFundamentals(8e9, 3), risk_blocklist=["EVIL"])
    assert snap.has_major_risk is True


def test_missing_data_is_safe_side():
    snap = load_fundamentals("AAPL", "US", lambda s, m: None)
    assert snap.market_cap == 0.0
    assert snap.positive_profit_quarters == 0


def test_source_exception_is_safe_side():
    def boom(s, m):
        raise RuntimeError("network")

    snap = load_fundamentals("AAPL", "US", boom)
    assert snap.market_cap == 0.0


def test_cache_avoids_second_source_call():
    calls: list[str] = []

    def source(s, m):
        calls.append(s)
        return RawFundamentals(8e9, 3)

    cache: dict = {}
    load_fundamentals("AAPL", "US", source, cache=cache)
    load_fundamentals("AAPL", "US", source, cache=cache)
    assert len(calls) == 1
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/test_fundamentals.py -q`
Expected: FAIL（`ModuleNotFoundError: quant.data.fundamentals`）

- [ ] **Step 3: 实现**

`backend/quant/data/fundamentals.py`:

```python
from __future__ import annotations

from dataclasses import dataclass
from typing import Callable, Iterable

from app.strategies.trend_portfolio import FundamentalSnapshot


@dataclass(frozen=True)
class RawFundamentals:
    market_cap: float
    positive_profit_quarters: int


FundamentalsSource = Callable[[str, str], "RawFundamentals | None"]


def _normalize(symbol: str) -> str:
    return symbol.strip().upper()


def load_fundamentals(
    symbol: str,
    market: str,
    source: FundamentalsSource,
    risk_blocklist: Iterable[str] = (),
    cache: dict[str, FundamentalSnapshot] | None = None,
) -> FundamentalSnapshot:
    key = _normalize(symbol)
    if cache is not None and key in cache:
        return cache[key]

    has_major_risk = key in {_normalize(item) for item in risk_blocklist}
    try:
        raw = source(symbol, market)
    except Exception:
        raw = None

    if raw is None:
        snapshot = FundamentalSnapshot(
            positive_profit_quarters=0,
            market_cap=0.0,
            has_major_risk=has_major_risk,
        )
    else:
        snapshot = FundamentalSnapshot(
            positive_profit_quarters=int(raw.positive_profit_quarters),
            market_cap=float(raw.market_cap),
            has_major_risk=has_major_risk,
        )

    if cache is not None:
        cache[key] = snapshot
    return snapshot


def default_fundamentals_source(symbol: str, market: str) -> RawFundamentals | None:
    # 真实网络实现：正确性以 Plan 3 本地实测为准（接口字段名可能需微调）。
    if market == "US":
        return _yfinance_fundamentals(symbol)
    if market == "HK":
        return _akshare_fundamentals(symbol)
    return None


def _yfinance_fundamentals(symbol: str) -> RawFundamentals | None:
    import yfinance as yf

    ticker = yf.Ticker(symbol)
    info = getattr(ticker, "info", None) or {}
    market_cap = float(info.get("marketCap") or 0.0)
    if market_cap <= 0:
        return None
    positive = 0
    income = getattr(ticker, "quarterly_income_stmt", None)
    if income is not None and "Net Income" in getattr(income, "index", []):
        for value in list(income.loc["Net Income"])[:2]:
            if value is not None and float(value) > 0:
                positive += 1
    return RawFundamentals(market_cap=market_cap, positive_profit_quarters=positive)


def _akshare_fundamentals(symbol: str) -> RawFundamentals | None:
    # 港股基本面接口在 Plan 3 本地确认 akshare 函数名与字段；
    # 取不到时返回 None（→ 安全侧，screen 不通过），不阻塞链路。
    try:
        import akshare as ak  # noqa: F401
    except Exception:
        return None
    return None
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/test_fundamentals.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/data/fundamentals.py backend/tests/quant/test_fundamentals.py
git commit -m "feat(quant): 基本面 adapter(可注入+缓存+风险黑名单)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 市场信息 provider(停牌/除权/公告)

**Files:**
- Create: `backend/quant/live/market_info.py`
- Test: `backend/tests/quant/live/test_market_info.py`

**Interfaces:**
- Produces:
  - `SymbolMarketInfo(halted: bool, ex_dividend_soon: bool)`
  - `MarketInfoSource = Callable[[str], SymbolMarketInfo | None]`
  - `MarketInfoProvider(source=None, news_blocklist=())`,`lookup(symbol) -> tuple[bool, bool, bool]`(返回 `halted, ex_dividend_soon, major_news`)。

- [ ] **Step 1: 写失败测试**

`backend/tests/quant/live/test_market_info.py`:

```python
from __future__ import annotations

from quant.live.market_info import MarketInfoProvider, SymbolMarketInfo


def test_no_source_does_not_block():
    assert MarketInfoProvider().lookup("AAPL") == (False, False, False)


def test_news_blocklist_flags_major_news():
    assert MarketInfoProvider(news_blocklist=["AAPL"]).lookup("aapl") == (False, False, True)


def test_source_values_passthrough():
    provider = MarketInfoProvider(source=lambda s: SymbolMarketInfo(halted=True, ex_dividend_soon=False))
    assert provider.lookup("AAPL") == (True, False, False)


def test_configured_source_unknown_symbol_is_conservative():
    provider = MarketInfoProvider(source=lambda s: None)
    assert provider.lookup("AAPL") == (True, False, False)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_market_info.py -q`
Expected: FAIL（`ModuleNotFoundError: quant.live.market_info`）

- [ ] **Step 3: 实现**

`backend/quant/live/market_info.py`:

```python
from __future__ import annotations

from dataclasses import dataclass
from typing import Callable, Iterable


@dataclass(frozen=True)
class SymbolMarketInfo:
    halted: bool
    ex_dividend_soon: bool


MarketInfoSource = Callable[[str], "SymbolMarketInfo | None"]


def _normalize(symbol: str) -> str:
    return symbol.strip().upper()


class MarketInfoProvider:
    def __init__(
        self,
        source: MarketInfoSource | None = None,
        news_blocklist: Iterable[str] = (),
    ) -> None:
        self.source = source
        self.news_blocklist = {_normalize(item) for item in news_blocklist}

    def lookup(self, symbol: str) -> tuple[bool, bool, bool]:
        """returns (halted, ex_dividend_soon, major_news)."""
        major_news = _normalize(symbol) in self.news_blocklist
        if self.source is None:
            return (False, False, major_news)
        try:
            info = self.source(symbol)
        except Exception:
            info = None
        if info is None:
            return (True, False, major_news)
        return (info.halted, info.ex_dividend_soon, major_news)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_market_info.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/market_info.py backend/tests/quant/live/test_market_info.py
git commit -m "feat(live): 市场信息 provider(停牌/除权/公告)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: data_provider 接入真实基本面与市场信息

**Files:**
- Modify: `backend/quant/live/data_provider.py`
- Test: `backend/tests/quant/live/test_data_provider.py`

**Interfaces:**
- Consumes: `load_fundamentals`、`FundamentalsSource`、`default_fundamentals_source`（Task 3）;`MarketInfoProvider`（Task 4）。
- Produces: `DefaultLiveDataProvider(market_data, symbols=None, daily_loader=None, today=None, fundamentals_source=None, risk_blocklist=(), market_info=None)`。`intraday_candidates()` 用 `market_info.lookup` 填 halted/ex_dividend_soon/major_news;`portfolio_rows()` 用 `load_fundamentals` 填真实基本面。`_default_fundamentals` 删除。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_data_provider.py`（沿用文件顶部的 `_daily` 助手）:

```python
def test_intraday_candidates_use_market_info():
    from quant.live.market_info import MarketInfoProvider, SymbolMarketInfo

    provider = DefaultLiveDataProvider(
        market_data=BarAggregator(),
        symbols=[SymbolInfo("AAPL", "Apple", "US")],
        daily_loader=_daily,
        today=date(2026, 6, 23),
        market_info=MarketInfoProvider(source=lambda s: SymbolMarketInfo(halted=True, ex_dividend_soon=False)),
    )

    assert provider.intraday_candidates()[0].halted is True


def test_portfolio_rows_use_real_fundamentals():
    from quant.data.fundamentals import RawFundamentals

    provider = DefaultLiveDataProvider(
        market_data=BarAggregator(),
        symbols=[SymbolInfo("0700.HK", "腾讯", "HK")],
        daily_loader=_daily,
        today=date(2026, 6, 23),
        fundamentals_source=lambda s, m: RawFundamentals(market_cap=6e9, positive_profit_quarters=4),
    )

    rows = provider.portfolio_rows()
    assert rows[0][3].market_cap == 6e9
    assert rows[0][3].positive_profit_quarters == 4
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_data_provider.py -q`
Expected: FAIL（`TypeError: __init__() got an unexpected keyword argument 'market_info'`）

- [ ] **Step 3: 实现**

`backend/quant/live/data_provider.py`:更新 import、构造函数、两个方法,删除 `_default_fundamentals`。

import 区追加:

```python
from typing import Callable, Iterable, Protocol

from quant.data.fundamentals import FundamentalsSource, default_fundamentals_source, load_fundamentals
from quant.live.market_info import MarketInfoProvider
```

`DefaultLiveDataProvider.__init__`:

```python
    def __init__(
        self,
        market_data: BarAggregator,
        symbols: list[SymbolInfo] | None = None,
        daily_loader: DailyLoader | None = None,
        today: date | None = None,
        fundamentals_source: FundamentalsSource | None = None,
        risk_blocklist: Iterable[str] = (),
        market_info: MarketInfoProvider | None = None,
    ) -> None:
        self.market_data = market_data
        self.symbols = symbols or all_symbols()
        self.daily_loader = daily_loader or load_daily
        self.today = today
        self.fundamentals_source = fundamentals_source or default_fundamentals_source
        self.risk_blocklist = tuple(risk_blocklist)
        self.market_info = market_info or MarketInfoProvider()
        self._fundamentals_cache: dict[str, FundamentalSnapshot] = {}
```

`intraday_candidates()` 里构造 `IntradayCandidate` 处改为:

```python
            halted, ex_dividend_soon, major_news = self.market_info.lookup(item.symbol)
            candidates.append(
                IntradayCandidate(
                    symbol=item.symbol,
                    market=item.market,
                    avg_turnover=float(turnover),
                    prev_amplitude_pct=round(amplitude, 4),
                    price=float(latest_price),
                    halted=halted,
                    ex_dividend_soon=ex_dividend_soon,
                    major_news=major_news,
                )
            )
```

`portfolio_rows()` 里 append 处改为:

```python
            fundamentals = load_fundamentals(
                item.symbol,
                item.market,
                self.fundamentals_source,
                self.risk_blocklist,
                self._fundamentals_cache,
            )
            rows.append((item.symbol, item.market, daily, fundamentals))
```

删除 `_default_fundamentals` 函数（不再引用)。`FundamentalSnapshot` 已通过现有 import 可用。

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_data_provider.py -q`
Expected: PASS（含原有两个测试）

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/data_provider.py backend/tests/quant/live/test_data_provider.py
git commit -m "feat(live): data_provider 接入真实基本面与停牌/除权/公告

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: 网关历史K线查询

**Files:**
- Modify: `backend/quant/live/gateway.py`
- Test: `backend/tests/quant/live/test_gateway.py`

**Interfaces:**
- Consumes: `Bar`（来自 `quant.live.market_data`）。
- Produces:
  - `FutuLiveGateway.query_history_minute(symbol, count=800, exchange=None) -> list[Bar]`（未连接抛 `RuntimeError`;vnpy import 延迟在 `_require_main_engine` 之后)。
  - 纯helper `_bars_from_vnpy(symbol: str, raw_bars) -> list[Bar]`（远程可测）。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_gateway.py`:

```python
def test_bars_from_vnpy_maps_fields():
    from datetime import datetime, timezone
    from quant.live.gateway import _bars_from_vnpy

    class _FakeBar:
        datetime = datetime(2026, 6, 23, 9, 30, tzinfo=timezone.utc)
        open_price = 1.0
        high_price = 2.0
        low_price = 0.5
        close_price = 1.5
        volume = 100.0

    bars = _bars_from_vnpy("AAPL", [_FakeBar()])
    assert bars[0].symbol == "AAPL"
    assert bars[0].high == 2.0
    assert bars[0].close == 1.5
    assert bars[0].volume == 100.0


def test_query_history_minute_requires_connection():
    import pytest

    from quant.live.config import FutuGatewayConfig
    from quant.live.gateway import FutuLiveGateway
    from quant.live.state import LiveGatewayState

    config = FutuGatewayConfig("127.0.0.1", 11111, "SIMULATE", "HK", True, False)
    gateway = FutuLiveGateway(config, LiveGatewayState())
    with pytest.raises(RuntimeError):
        gateway.query_history_minute("AAPL")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_gateway.py -q`
Expected: FAIL（`ImportError: cannot import name '_bars_from_vnpy'`）

- [ ] **Step 3: 实现**

`backend/quant/live/gateway.py`:顶部 import 追加 `from quant.live.market_data import Bar`,在类中加方法、文件末尾加 helper。

```python
    def query_history_minute(
        self,
        symbol: str,
        count: int = 800,
        exchange: str | None = None,
    ) -> list[Bar]:
        # 先校验连接（未连接抛 RuntimeError），再延迟 import vnpy（远程无 vnpy）。
        main_engine = self._require_main_engine()
        from datetime import datetime, timedelta

        from vnpy.trader.constant import Exchange, Interval
        from vnpy.trader.object import HistoryRequest

        req = HistoryRequest(
            symbol=_clean_symbol(symbol),
            exchange=_resolve_exchange(symbol, self.config.market, Exchange, exchange),
            start=datetime.now() - timedelta(days=5),
            end=datetime.now(),
            interval=Interval.MINUTE,
        )
        raw_bars = main_engine.query_history(req, _GATEWAY_NAME) or []
        return _bars_from_vnpy(symbol, raw_bars)[-count:]
```

文件末尾追加 helper:

```python
def _bars_from_vnpy(symbol: str, raw_bars) -> list[Bar]:
    bars: list[Bar] = []
    for item in raw_bars:
        bars.append(
            Bar(
                symbol=symbol,
                start=getattr(item, "datetime", None),
                open=float(getattr(item, "open_price", 0.0)),
                high=float(getattr(item, "high_price", 0.0)),
                low=float(getattr(item, "low_price", 0.0)),
                close=float(getattr(item, "close_price", 0.0)),
                volume=float(getattr(item, "volume", 0.0)),
            )
        )
    return bars
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_gateway.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/quant/live/gateway.py backend/tests/quant/live/test_gateway.py
git commit -m "feat(live): 网关历史分钟K线查询(补种用)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: runtime 订阅时历史补种

**Files:**
- Modify: `backend/quant/live/runtime.py`
- Test: `backend/tests/quant/live/test_runtime.py`

**Interfaces:**
- Consumes: `gateway.query_history_minute()`（Task 6,可选——网关无此方法时跳过);`market_data.seed_minute_bars()`（Task 2)。
- Produces: `LiveRuntime._seed_history(symbols: list[str]) -> None`,在盘前选股/月末选股订阅后调用;每标的每交易日补种一次(`_seeded_symbols` 按日重置)。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/quant/live/test_runtime.py`:

```python
def test_seed_history_populates_market_data_once(tmp_path):
    from quant.live.market_data import BarAggregator

    calls: list[str] = []

    class SeedGateway(DryRunGateway):
        def query_history_minute(self, symbol, count=800, exchange=None):
            calls.append(symbol)
            return [
                Bar(symbol, datetime(2026, 6, 23, 9, m, tzinfo=timezone.utc), 100, 100, 100, 100, 100)
                for m in range(20)
            ]

    live_state = LiveGatewayState()
    gateway = SeedGateway(live_state)
    market_data = BarAggregator()
    runtime = LiveRuntime(
        live_state=live_state,
        gateway=gateway,
        scheduler=LiveScheduler(markets=()),
        data_provider=FakeDataProvider(),
        market_data=market_data,
        runtime_state=StrategyRuntimeState(),
        config=RuntimeConfig(enabled=True, dry_run=True),
        db_path=tmp_path / "live.sqlite3",
    )
    runtime._seeded_day = datetime(2026, 6, 23, tzinfo=timezone.utc).date()

    runtime._seed_history(["AAPL"])
    runtime._seed_history(["AAPL"])

    assert market_data.minute_bars("AAPL")
    assert calls == ["AAPL"]  # 当日只补种一次


def test_seed_history_skips_gateway_without_history(tmp_path):
    runtime, _gateway = _runtime(tmp_path)  # DryRunGateway 无 query_history_minute
    runtime._seed_history(["AAPL"])  # 不抛异常即可
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: FAIL（`AttributeError: 'LiveRuntime' object has no attribute '_seed_history'`）

- [ ] **Step 3: 实现**

`backend/quant/live/runtime.py`:

1) `LiveRuntime.__init__` 末尾加两个字段:

```python
        self._seeded_day = None
        self._seeded_symbols: set[str] = set()
```

2) `run_once` 开头(在 `reset_for_day` 之后)加按日重置:

```python
    def run_once(self, at: datetime) -> None:
        snapshot = self.live_state.snapshot()
        self.runtime_state.reset_for_day(at.date())
        if self._seeded_day != at.date():
            self._seeded_day = at.date()
            self._seeded_symbols.clear()
        self.market_data.ingest_ticks(snapshot.get("ticks", []))
        ...
```

3) 新增方法:

```python
    def _seed_history(self, symbols: list[str]) -> None:
        query = getattr(self.gateway, "query_history_minute", None)
        if query is None:
            return
        for symbol in symbols:
            if symbol in self._seeded_symbols:
                continue
            try:
                bars = query(symbol)
            except Exception as exc:
                self._record_log("runtime", f"{symbol} 历史补种失败：{exc}")
                continue
            self.market_data.seed_minute_bars(symbol, bars)
            self._seeded_symbols.add(symbol)
```

4) 在 `_run_intraday_premarket_scan` 与 `_run_portfolio_month_end_scan` 里 `self.gateway.subscribe(symbols)` 之后各加一行 `self._seed_history(symbols)`:

```python
        if symbols:
            self.gateway.subscribe(symbols)
            self._seed_history(symbols)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/quant/live/test_runtime.py -q`
Expected: PASS

- [ ] **Step 5: 全量回归 + 提交**

```bash
.venv/bin/python -m pytest -q
# Expected: all green（约 104 + 新增用例）
git add backend/quant/live/runtime.py backend/tests/quant/live/test_runtime.py
git commit -m "feat(live): 订阅时历史分钟补种,每标的每日一次

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage（对照 spec 第 4 节 Plan 1）**
- 4.1 历史分钟补种 → Task 2(seed)+ Task 6(网关历史)+ Task 7(订阅时补种) ✓
- 4.2 基本面 adapter → Task 3 + Task 5(接线) ✓
- 4.3 停牌/除权/公告 → Task 4 + Task 5(接线) ✓
- 4.4 可做空名单 → Task 1 ✓
- 4.5 测试(注入式 fake) → 每个 Task 的 Step 1 ✓

**2. Placeholder scan:** 无 TBD/TODO。`default_fundamentals_source` 的网络实现是真实代码,其 live 正确性按 spec 明确归属 Plan 3 本地验证(非占位)。

**3. Type consistency:**
- `RawFundamentals(market_cap, positive_profit_quarters)` — Task 3 定义,Task 5 测试一致。
- `MarketInfoProvider.lookup -> (halted, ex_dividend_soon, major_news)` — Task 4 定义,Task 5 接线一致。
- `seed_minute_bars(symbol, bars)` — Task 2 定义,Task 7 调用一致。
- `query_history_minute(symbol, count, exchange)` / `_bars_from_vnpy(symbol, raw_bars)` — Task 6 定义,Task 7 通过 `getattr` 可选调用一致。
- `DefaultLiveDataProvider` 新参数 `fundamentals_source/risk_blocklist/market_info` — Task 5 定义并自测。

**边界说明:** `build_live_runtime_from_env` 现以 `DefaultLiveDataProvider(market_data)` 构造,默认走真实源;运行时默认 `enabled=False`,远程不触发真实源调用,无需改动。真实源连通在 Plan 3 验证。
